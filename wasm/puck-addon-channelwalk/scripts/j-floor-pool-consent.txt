# SUPERSEDED by owner ruling 2026-08-02 (headless P6b, trusted-by-authorship re-cut) -- see
# .claude/skills/puck-world/references/authority.md.
# `addon:channelwalk` is a DOCUMENT-MOUNTED (Simulation-lane) addon, so its channel contributions are now WORLD
# LOGIC: TRUSTED in the fold, added OUTSIDE the pool, gated by its own declared Reach ONLY -- consent does not
# apply to world logic. Every comment below this line was written against the PRIOR pooled-under-consent model,
# where a bare `channels:strafe` Drive row (no seat-authored ceiling) folded nothing at all. Re-run verbatim
# post-recut (this file's own commands unchanged): ALL FOUR `player.channels` readings below now read IDENTICALLY
# -- `strafe:bipolar folded=1(65536) ... composed=1(65536) trusted=[addon:channelwalk] untrusted=[] ceiling=none
# clamped=no` -- regardless of whether `seat1`'s ceiling row exists at all, and regardless of its value (1.0,
# 0.25, or 0.2500152587890625). The pool-ceiling boundary pair this file used to demonstrate on `strafe` is now
# INERT for a document-mounted addon: `seat1`'s ceiling grant still succeeds (nothing here refuses it) but no
# longer bounds anything a trusted addon contributes -- a ceiling only ever gates a GENUINELY untrusted (pooled)
# contributor (a Peer today; a future client-hosted addon). This script is kept AS A RECORD of that flip -- run it
# to see the four identical readings yourself -- not as a still-accurate walkthrough of the old numbers.
#
# `strafe` (not `trigger`/`forward`) is used here because it is the ONE channel channelwalk contributes to
# forever, with no expiry window -- the pool/consent gate (now: the trusted/reach gate) can be tested at any point
# long after mount, unlike `forward` (a finite window) or `trigger` (settles and stops).
#
# --world must be an absolute path to worlds/channel-walk-world.json. Substitute <ABS_WORLD_PATH> before running.
replay.status
world.wait 5
# Reach only, no seat-authored ceiling -- and none is needed post-recut: a document-mounted addon's own declared
# Reach is the whole gate now (trusted-by-authorship), so this line alone is enough to land the contribution.
world.grant addon:channelwalk drive body:0 budget:60 channels:strafe
world.wait 300
# Expect strafe folded=1(65536), composed=1(65536), trusted=[addon:channelwalk], untrusted=[] -- the addon's
# fixed::ONE contribution lands IN FULL with NO consent ever authored (the P6b headline behavior).
player.channels 1
# A seat ceiling on the SAME channel still grants successfully (Conflicts never refused this shape), but is now a
# no-op for this addon's own term -- there is no pool left for it to bound.
world.grant seat1 drive body:0 channels:strafe ceiling:1.0
world.wait 5
# Expect: IDENTICAL to the reading above -- the ceiling changed nothing.
player.channels 1
# A LOW ceiling (well under the addon's fixed::ONE contribution) -- under the prior pooled model this would have
# clamped the composed value to exactly this ceiling.
world.grant seat1 drive body:0 channels:strafe ceiling:0.25
world.wait 5
# Expect: STILL folded=1(65536), composed=1(65536), clamped=no -- a document-mounted addon's trusted term is never
# clamped by a seat's pool ceiling, no matter how far below its contribution the ceiling sits.
player.channels 1
# The old ceiling-boundary pair's second half (c+1 raw) -- kept for continuity; equally inert now.
world.grant seat1 drive body:0 channels:strafe ceiling:0.2500152587890625
world.wait 5
# Expect: STILL folded=1(65536), composed=1(65536), clamped=no.
player.channels 1
world.grants seat1
world.grants addon:channelwalk

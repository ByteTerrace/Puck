# Every frame renders every pass, and the film grain post pass composes over each one.
# The engine ready first: its pipeline set installed and its first frame produced, however long a cold driver
# cache takes.
world.wait ready 45
world.cadence off
world.wait 30
world.screenshot {run}/frame.png
world.wait 10

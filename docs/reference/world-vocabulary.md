# World vocabulary

Generated by `puck vocabulary` from `WorldConstructTable`; do not hand-edit.
`puck vocabulary --check` fails when this file disagrees with the table.

Every construct of `puck.world.definition.v1`'s authoring vocabulary: the word at the start of
the line, the members it carries, the document member it lowers to, and what the printer needs
before it may print a document node back as that construct. What a document field means is
`puck schema`'s (`src/Puck.World/Assets/worlds/schema/`), and which fields carry names is
`puck registry`'s ([the name registry](../world-name-registry.md)); this table is the surface and
its mapping onto them.

A `Lowers to` path is read from the document's own root, and a trailing `[]` marks one element of
an array. `Written in` names the innermost construct whose body admits the row; `rule` there means
a rule body wherever one appears — a `rule`, an `option`, a `step`, or an `if`/`transaction`
branch. `Admitted on` is the cell kinds the row's own `kind` must be one of before the member is
admitted — a range and a per-second accumulation are defined over the numeric kinds alone, and a
row of any other kind refuses them by name — and is empty for a member whose admission turns on no
kind.

## Constructs

| Keyword | Written in | Shape | Lowers to | Declares |
|---|---|---|---|---|
| `addon` | the document | row | `addons[]` | One WebAssembly extension, its capability requests, and its machine-memory watches. |
| `asset` | the document | statement | `(nothing)` | Resolves a package asset wherever a document value is accepted. |
| `border` | the document | block | `(nothing)` | Connects two world boundaries with a bidirectional border window. |
| `cartridge` | the document | section | `cartridge` | The cartridge a hosted machine in this world runs. |
| `derive` | the document | statement | `(nothing)` | Names an operand at compile time; the document carries the expansion, never the name. |
| `door` | the document | statement | `(nothing)` | Connects a door face to a named arrival point in another world. |
| `enum` | the document | block | `(nothing)` | Names a run of integers at compile time; the document carries the numbers, never the enumeration. |
| `ground` | the document | block | `(nothing)` | Generates a solid ground prototype, its placement, and boundary geometry. |
| `host` | the document | section | `host` | The window, its presentation mode, and the simulation tick rate. |
| `material` | the document | row | `materials[]` | One surface material. |
| `pairPool` | the document | declaration | `state.pairPools[]` | Declares a bounded pool of relationships between two ordinary pools. |
| `pattern` | the document | block | `patterns[]` | One pattern over a row's tokens, compiled while it lowers so a refusal names the line that declared it. |
| `placements` | the document | section | `placements` | Where prototypes stand in the world. |
| `pool` | the document | declaration | `state.pools[]` | Declares a bounded typed instance pool with optional static seeds. |
| `prototypes` | the document | section | `prototypes` | The creation documents this world places. |
| `record` | the document | block | `(nothing)` | A typed field set instantiated by pools. |
| `rule` | the document | block | `rules[]` | One reactive rule: a gate, its working values, and the effects it fires. |
| `rules` | the document | block | `rules[]` | A shared header for several rules: its gate, locals and properties apply to every member. |
| `set` | the document | statement | `sets[]` | One named set over the positions of a board, a zone, or a family, which `boardCombine` and `writeSet` read by name. |
| `shape` | the document | row | `shapes[]` | One signed-distance shape of a creation document. |
| `spawn` | the document | block | `(nothing)` | Generates a named arrival point that a door can target. |
| `sql` | the document | embedded language | `state` | A hermetic SQL spelling of rows and rules, parsed by its own dialect and lowered to the same document members the native constructs write. |
| `stabilize` | the document | block | `ruleGroups[]` | A fixpoint group: its members re-fire until the board settles or the pass ceiling refuses. |
| `state` | the document | section | `state` | The simulation state: the world's rows, its lattices, its embedding spaces, and the per-participant lanes. |
| `test` | the document | block | `(nothing)` | Behaviour stated in the world's own language — a world's own, a module's under the arguments the test gives it, or a whole composition's; the document carries no trace of it and each test lowers to a generated test world of its own. |
| `views` | the document | section | `views` | Camera layouts, render pipelines, frame-graph instances, and the seat rig a participant looks through. |
| `workflow` | the document | block | `ruleGroups[]` | A staged group: a cursor that advances one step per committed firing. |
| `world` | the document | statement | `(nothing)` | Emits one named world document by invoking a module, declared at the top level of its source. Written `entry world`, it is the world `Puck.World --world` boots when it boots the source; a composition declares at most one. |
| `request` | `addon` | statement | `addons[].requests[]` | One capability an addon declares it needs. |
| `watchMemory` | `addon` | statement | `addons[].memoryWatches[]` | A span of a hosted machine's memory an addon observes. |
| `interrupt` | `decision` | statement | `rules[].decision.interrupt` | What cuts a commitment short. |
| `onNoChoice` | `decision` | block | `rules[].decision.onNoChoice` | What fires when no option's gate admits it. |
| `option` | `decision` | block | `rules[].decision.options[]` | One candidate of a decision. |
| `placement` | `placements` | row | `placements.rows[]` | One placed prototype. |
| `prototype` | `prototypes` | row | `prototypes[]` | One creation document under an identity. |
| `claim` | `rule` | block | `rules[].effects[][claim]` | Claims one available instance and binds its fields for nested effects. |
| `claim pair` | `rule` | block | `rules[].effects[][claimPair]` | Claims one relationship between two bound record instances. |
| `deal` | `rule` | statement | `rules[].effects[][transformState]` | Moves a counted run of tokens from the front of an ordered zone to another. |
| `decision` | `rule` | block | `rules[].decision` | A reconsidered choice among scored options. |
| `draw` | `rule` | statement | `rules[].effects[][transformState]` | Moves one token from the front of an ordered zone to another. |
| `for each` | `rule` | block | `rules[].effects[][forEachPool]` | Visits every live instance in ascending slot order. |
| `if` | `rule` | block | `rules[].effects[][if]` | A conditional effect. |
| `local` | `rule` | statement | `rules[].locals[]` | One working value the rule computes before its effects. |
| `push` | `rule` | statement | `rules[].effects[][pushState]` | Appends a value to a history ring, overwriting the oldest slot when the ring is full. |
| `release` | `rule` | statement | `rules[].effects[][release]` | Releases the instance named by a lexical pool binding. |
| `remove` | `rule` | statement | `rules[].effects[][removeStateCell]` | Drops a cell from its row; a later read of that key reads zero. |
| `schedule` | `rule` | statement | `rules[].effects[][scheduleState]` | Writes a cell a fixed time from now. |
| `shuffle` | `rule` | statement | `rules[].effects[][transformState]` | Reorders a row's cells by one pass over a draw row. |
| `transaction` | `rule` | block | `rules[].effects[][transaction]` | A batch of effects that commits as one and rewinds as one. |
| `transform` | `rule` | statement | `rules[].effects[][transformState]` | Runs one state transform. |
| `when` | `rule` | statement | `rules[].gate` | What must hold before the rule fires. |
| `space` | `spaces` | row | `state.spaces[]` | One vector embedding space. |
| `spaces` | `state` | section | `state.spaces` | The document's named vector embedding spaces, declared before any row names one. |
| `world` | `state` | section | `state.world` | The authoritative rows, written as declarations; the second authoring of the section in either form is refused. |
| `onFailure` | `transaction` | block | `rules[].effects[][transaction].onFailure` | What fires after a transaction rewinds; more than one on a transaction is refused. |
| `graph` | `views` | row | `views.graphs[]` | One named instance of a frame graph. |
| `layout` | `views` | row | `views.layouts[]` | One viewport arrangement. |
| `pipeline` | `views` | row | `views.pipelines[]` | One named post-render pipeline. |
| `seatControl` | `views` | section | `views.seatControl` | How a participant's input moves the seat rig. |
| `seatRig` | `views` | section | `views.seatRig` | The camera program a seated participant sees through. |
| `step` | `workflow` | block | `rules[]` | One stage of a workflow; its body is a rule body, so a nested `rule` block is refused. |
| `grid` | `world` | declaration | `state.world[]` | A board and the Grid topology under it, from one statement. |
| `pile` | `world` | declaration | `state.world[]` | An ordered-membership row over another row's keys; a pile is always Bool, so it carries no kind. |
| `row` | `world` | section | `state.world[]` | The explicit row form, carrying every field the four declarations cannot spell. |
| `slot` | `world` | declaration | `state.world[]` | A one-cell row, written as its value rather than as the explicit `value` field. |
| `table` | `world` | declaration | `state.world[]` | A keyed row, written as its cells rather than as the explicit `cells` array. |

## Members, and what the printer requires

### `addon`

```puck
addon ["name"] { modulePath: … hash: … request … watchMemory … }
```

| Member | Written | Kind | Admitted on | Lowers to | Default | Means |
|---|---|---|---|---|---|---|
| `name` | header | name | — | `name` | — | The addon's name. |
| `hash:` | property | text | — | `hash` | — | The module's content hash; `auto` is filled in from the module beside the source. |

Printed back as `addon`; otherwise the generic value path. Every field of the node the description does not name prints as an ordinary property.

### `asset`

```puck
asset "package-relative/path"
```

| Member | Written | Kind | Admitted on | Lowers to | Default | Means |
|---|---|---|---|---|---|---|
| `path` (required) | header | text | — | the locked content path stored in the surrounding document field | — | The source-relative asset path resolved through the package's asset lock. |

Never printed back; the printer produces the statements its expansion produced in its place.

### `border`

```puck
border left.endpoint, right.endpoint { [center [x, y, z]] [yaw: angle] [pitch: angle] [width: length] [height: length] [hysteresis: length] }
```

| Member | Written | Kind | Admitted on | Lowers to | Default | Means |
|---|---|---|---|---|---|---|
| `left` (required) | header | reference | — | adjacency and boundary fields in the two endpoint worlds | — | The source world and its boundary endpoint. |
| `right` (required) | header | reference | — | adjacency and boundary fields in the two endpoint worlds | — | The destination world and its boundary endpoint. |
| `center:` | property | point | — | the border window's center | — | The optional center of the border window. |
| `yaw:` | property | value | — | the border window's yaw | — | The optional heading of the border window. |
| `pitch:` | property | value | — | the border window's pitch | — | The optional pitch of the border window. |
| `width:` | property | length | — | the border window's width | — | The positive width of the border window. |
| `height:` | property | length | — | the border window's height | — | The positive height of the border window. |
| `hysteresis:` | property | length | — | the border transition's hysteresis | — | The nonnegative distance that stabilizes transitions across the border. |

Read back as `border` from the rows it generated when the composition's worlds are decompiled together and the two adjacencies, references and destinations the border generated regenerate from it exactly; otherwise a refusal naming the generated row, since printing it as an authored name writes a source the compiler refuses.

### `cartridge`

```puck
cartridge { rom: … hash: … }
```

| Member | Written | Kind | Admitted on | Lowers to | Default | Means |
|---|---|---|---|---|---|---|
| `hash:` | property | text | — | `hash` | — | The ROM's content hash; `auto` is filled in from the file beside the source. |

Printed back as `cartridge`; otherwise the generic value path. Every field of the node the description does not name prints as an ordinary property.

### `derive`

```puck
derive name = <expression>
```

| Member | Written | Kind | Admitted on | Lowers to | Default | Means |
|---|---|---|---|---|---|---|
| `name` (required) | header | name | — | `name` | — | The derived name, substituted wherever an operand reads it. |
| `expression` (required) | header | operand | — | the operand text substituted wherever the name is read | — | The operand text every read of the name expands to. |

Never printed back; the printer produces the statements its expansion produced in its place.

### `door`

```puck
door left.endpoint, right.spawn
```

| Member | Written | Kind | Admitted on | Lowers to | Default | Means |
|---|---|---|---|---|---|---|
| `left` (required) | header | reference | — | door topology and travel fields in the two worlds | — | The source world and its door face. |
| `right` (required) | header | reference | — | door topology and travel fields in the two worlds | — | The destination world and its named spawn point. |

Read back as `door` from the rows it generated when the composition's worlds are decompiled together and the portals, return arch, references and destinations the door generated regenerate from it exactly; otherwise a refusal naming the generated row, since printing it as an authored name writes a source the compiler refuses.

### `enum`

```puck
enum Name { member … }
```

| Member | Written | Kind | Admitted on | Lowers to | Default | Means |
|---|---|---|---|---|---|---|
| `name` (required) | header | name | — | `name` | — | The enumeration's name, which qualifies each member as `Name.member`. |
| `members` (required) | body | statements | — | the literals each read of a member expands to | — | The members, each a compile-time constant equal to its own 0-based position. |

Never printed back; the printer produces the statements its expansion produced in its place.

### `ground`

```puck
ground name { size [width, depth] [center [x, y, z]] }
```

| Member | Written | Kind | Admitted on | Lowers to | Default | Means |
|---|---|---|---|---|---|---|
| `name` (required) | header | name | — | the generated prototype and placement identities | — | The ground patch's identity and the name used by automatic border endpoints. |
| `size:` (required) | property | value | — | the generated box scale and boundary dimensions | — | The required positive width and depth. |
| `center:` | property | point | — | the generated placement position and boundary center | — | The optional center, defaulting to the origin. |

Read back as `ground` from the rows it generated when its `ground$name` prototype and `name` placement are exactly the rows the block writes, standing in the same order in both sections; otherwise a refusal naming the generated row, since printing it as an authored name writes a source the compiler refuses.

### `host`

```puck
host { presentation: … width: … height: … fullscreen: … }
```

Printed back as `host`; otherwise the generic value path. Every field of the node the description does not name prints as an ordinary property.

### `material`

```puck
material ["name"] { … }
```

| Member | Written | Kind | Admitted on | Lowers to | Default | Means |
|---|---|---|---|---|---|---|
| `name` | header | name | — | `name` | — | The material's name, which a shape's `material` reads it by. |

Printed back as `material`; otherwise the generic value path. Every field of the node the description does not name prints as an ordinary property.

### `pairPool`

```puck
pairPool name record Record left Pool right Pool maxLive n directed flag allowSelf flag
```

| Member | Written | Kind | Admitted on | Lowers to | Default | Means |
|---|---|---|---|---|---|---|
| `name` (required) | header | name | — | `name` | — | The pair pool's stable name. |
| `record` (required) | header | reference | — | `record` | — | The record type shared by every relationship. |
| `left` (required) | header | reference | — | `leftPool` | — | The pool supplying each left endpoint. |
| `right` (required) | header | reference | — | `rightPool` | — | The pool supplying each right endpoint. |
| `maxLive` (required) | header | number | — | `maxLive` | — | The positive maximum number of live relationships. |
| `directed` | header | flag | — | `directed` | — | Whether endpoint order distinguishes relationships. |
| `allowSelf` | header | flag | — | `allowSelf` | — | Whether both endpoints may name the same instance. |

Never printed back; the printer produces the statements its expansion produced in its place.

### `pattern`

```puck
pattern name : Kind { [attribute: … | value: …] [maxStates: n] symbols { name = value … } match: <language> }
```

| Member | Written | Kind | Admitted on | Lowers to | Default | Means |
|---|---|---|---|---|---|---|
| `name` (required) | header | name | — | `name` | — | The pattern's name, which a match operand and a transform read it by. |
| `kind` (required) | header | cellkind (`Int`, `Fixed`, `Bool`, `Text`, `Vector`) | — | `kind` | — | The domain every cell of the row holds a value in. |
| `attribute:` | property | reference | — | `attribute` | — | The row supplying each token's word, in place of `value`. |
| `value:` | property | operand | — | `value` | — | An infix expression evaluated once per token to produce its word. |
| `maxStates:` | property | number | — | `maxStates` | — | The derivative machine's state budget, inside 1 to 1,024. |
| `symbols` (required) | body | statements | — | `symbols` | — | The alphabet, each entry a name standing for one value or a band. |
| `match:` (required) | property | text | — | `pattern` | — | The pattern language, loosest operator first: choice, intersection, sequence, complement, repetition; it lowers to the compiled tree. |

Printed back as `pattern` when the node carries `kind`, `name`, `pattern`, `symbols`, and it carries no key outside `attribute`, `kind`, `maxStates`, `name`, `pattern`, `symbols`, `value`, and its name reads as a bare identifier, its kind is spelled exactly as its enum member, its alphabet is non-empty and every symbol name is bare, and every n-ary node of its language carries two or more items; otherwise the generic value path, for the whole `patterns` array.

### `placements`

```puck
placements { policy { … } placement "id" { … } }
```

| Member | Written | Kind | Admitted on | Lowers to | Default | Means |
|---|---|---|---|---|---|---|
| `body` | body | statements | — | `rows` | — | Placement rows and the section's own properties; anything else in it is refused rather than dropped. |

Printed back as `placements` when every row in it sugars as `placement`; otherwise the generic value path. Every field of the node the description does not name prints as an ordinary property.

### `pool`

```puck
pool name of Record capacity(capacity) = [{ field: value … } …]
```

| Member | Written | Kind | Admitted on | Lowers to | Default | Means |
|---|---|---|---|---|---|---|
| `name` (required) | header | name | — | `name` | — | The pool's stable name. |
| `record` (required) | header | reference | — | `record` | — | The record type shared by every instance. |
| `slots` (required) | header | number | — | `capacity` | — | The positive maximum number of simultaneously live instances. |
| `initial` | body | value | — | `initial` | — | Static slot seeds, each overriding selected record fields. |

Never printed back; the printer produces the statements its expansion produced in its place.

### `prototypes`

```puck
prototypes { prototype "id" { document { … } } }
```

| Member | Written | Kind | Admitted on | Lowers to | Default | Means |
|---|---|---|---|---|---|---|
| `body` | body | statements | — | the entries of the `prototypes` array itself | — | Prototype rows only — the section lowers to a bare array, so there is no object for a property to land on. |

Printed back as `prototypes` when every row in it sugars as `prototype`; otherwise the generic value path. Every field of the node the description does not name prints as an ordinary property.

### `record`

```puck
record Name { field: Kind … }
```

| Member | Written | Kind | Admitted on | Lowers to | Default | Means |
|---|---|---|---|---|---|---|
| `name` (required) | header | name | — | `name` | — | The record's name, which a `pool name of Name` declaration instantiates. |
| `fields` (required) | body | statements | — | one field descriptor in `state.records[]` | — | The fields that every instance of a pool using this record carries. |

Never printed back; the printer produces the statements its expansion produced in its place.

### `rule`

```puck
rule "name" { [when Gate] [mode: …] [forEach: …] [local …] [decision { … }] <effect>* }
```

| Member | Written | Kind | Admitted on | Lowers to | Default | Means |
|---|---|---|---|---|---|---|
| `name` (required) | header | name | — | `name` | — | The rule's name, which a group claims it by and a trace reports. |
| `mode:` | property | enumeration (`Edge`, `Level`) | — | `mode` | — | Whether the rule fires on the gate becoming true or on every tick it is true. |
| `forEach:` | property | reference | — | `forEach` | — | The row whose live keys the rule iterates, one firing per key. |
| `effects` (required) | body | statements | — | `effects` | — | The effect statements the rule fires, in written order; a rule with none is refused. |

Printed back as `rule` when the node carries `effects`, `name`, and every predicate and effect target in it has a sugar spelling that reparses to the same tree; otherwise the generic value path, for the whole `rules` array. Every field of the node the description does not name prints as an ordinary property.

### `rules`

```puck
rules name [when Gate] { rule "name" { … } … }
```

| Member | Written | Kind | Admitted on | Lowers to | Default | Means |
|---|---|---|---|---|---|---|
| `name` (required) | header | name | — | `name` | — | The scope's name, prefixed onto every rule it claims. |
| `when` | header | gate | — | `gate` | — | A gate applied to every member, conjoined with the member's own. |

Read back as `rules` from the rows it generated when a rule's or group's name joins scope names to its own with `$` and every part is a name: each member prints inside one `rules` scope per part, carrying its own copy of the shared header; otherwise a refusal naming the generated row, since printing it as an authored name writes a source the compiler refuses.

### `set`

```puck
set name: <expression>
```

| Member | Written | Kind | Admitted on | Lowers to | Default | Means |
|---|---|---|---|---|---|---|
| `name` (required) | header | name | — | `name` | — | The set's name, which a transform and a search plan read it by. |
| `set` (required) | header | text | — | `set` | — | The cell-set algebra, loosest operator first: union, intersection, complement, over `all`, `none`, `board`, `zone` and `family`. |

Printed back as `set` when the node carries `name`, `set`, and it carries no key outside `name`, `set`, and the algebra reads the row back and prints it, and its name reads as a bare identifier; otherwise the generic value path, for the whole `sets` array.

### `shape`

```puck
shape Type ["name"] { … }
```

| Member | Written | Kind | Admitted on | Lowers to | Default | Means |
|---|---|---|---|---|---|---|
| `type` (required) | header | name | — | `type` | — | The signed-distance primitive or blend the shape is. |
| `name` | header | name | — | `name` | — | The shape's name, which a sibling's `parent` reads it by; an authored JSON null has no header spelling and prints as a property. |
| `id:` | property | number | — | `id` | `$index` | The shape's stable identity inside its document. |
| `blend:` | property | name | — | `blend` | `"Union"` | How the shape combines with what is already there. |
| `smooth:` | property | fixed | — | `smooth` | `0` | The blend's smoothing radius. |
| `rotation:` | property | value | — | `rotation` | `[0, 0, 0, 1]` | The shape's orientation quaternion; a binding reference in its place never elides. |
| `scale:` | property | value | — | `scale` | `[1, 1, 1]` | The shape's per-axis scale. |

Printed back as `shape` when the node carries `type`, and every row in the array carries its own `type`, so none of them is a basis-merge patch naming only the facets it overrides; otherwise the generic value path, for the whole `shapes` array. Every field of the node the description does not name prints as an ordinary property.

### `spawn`

```puck
spawn name { at [x, y, z] [yaw: angle] }
```

| Member | Written | Kind | Admitted on | Lowers to | Default | Means |
|---|---|---|---|---|---|---|
| `name` (required) | header | name | — | the generated spawn point's id | — | The arrival point's identity. |
| `at:` (required) | property | point | — | the generated spawn point's position | — | The required arrival position. |
| `yaw:` | property | value | — | the generated spawn point's yawDegrees | — | The optional arrival heading, defaulting to zero. |

Never printed back; the printer produces the statements its expansion produced in its place.

### `sql`

```puck
sql { <the state SQL dialect> }
```

Never printed back; the printer produces the native constructs the block expanded into in its place.

### `stabilize`

```puck
stabilize name [undo({ rows [row] depth: n })] [maxPasses(n)] [until Gate] { rule "name" { … } … }
```

| Member | Written | Kind | Admitted on | Lowers to | Default | Means |
|---|---|---|---|---|---|---|
| `name` (required) | header | name | — | `name` | — | The group's name, prefixed onto every rule it claims. |
| `undo(…)` | modifier | value | — | `undo` | — | The rows and bounded number of completed turns retained for rewindGroup. |
| `maxPasses(…)` | modifier | number | — | `passes` | — | The pass ceiling whose breach is one counted refusal naming the group. |
| `until` | header | gate | — | `trigger` | — | Arms the group while the gate reads false, so it lowers as that gate's negation. |
| `members` (required) | body | statements | — | `steps` | — | The rules the group claims, named `<group>$<rule>`; a group with no member is refused. |

Printed back as `stabilize` when the node carries `name`, `shape`, `steps`, and every rule it claims is in the document's own `rules` array and no other group claims it; otherwise the explicit `ruleGroups` array beside an unclaimed `rules` array. Every field of the node the description does not name prints as an ordinary property.

### `state`

```puck
state { world { … } [spaces { … }] [lattices [ … ]] [body [ … ]] [identity [ … ]] }
```

| Member | Written | Kind | Admitted on | Lowers to | Default | Means |
|---|---|---|---|---|---|---|
| `world` | body | statements | — | `world` | — | The world's own rows, as declarations or as the explicit array. |

Printed back as `state`; otherwise the generic value path, which carries every member unchanged. Every field of the node the description does not name prints as an ordinary property.

### `test`

```puck
test "name" [with module(arguments)] { given { row = literal | world { row = literal … } … } when { ticks n | seatN [refused ["text"]]: command | world { seatN: command … } … } expect { gate | world { gate … } … } }
```

| Member | Written | Kind | Admitted on | Lowers to | Default | Means |
|---|---|---|---|---|---|---|
| `name` (required) | header | text | — | the generated test world's file name, its verdict row names and its rule names | — | The test's name, which is what a failing verdict is addressed by. |
| `subject` | header | value | — | the whole generated test world: the named module expanded with these arguments, standing alone rather than inside the enclosing document | — | The module the test is about, written `with module(arguments)`; omitted for a test whose subject is the world it stands in. |
| `given` | body | cells | — | the generated test world's own `state.world[].cells[].value`, kind-checked against the row each names | — | The cells the generated test world boots at, one `row = literal` or `row[key] = literal` per line. |
| `when` | body | statements | — | the generated test world's `schedule` section: one row per seat step at the tick the preceding `ticks` reached, a `refused` step carrying `expect: Refused` and its `refusal` text, and the export tick the grid ends on | — | The tick grid and the commands along it: `ticks n` carries the cursor, `seatN: <command line>` submits one admitted step or read verb at it, and `seatN refused "text": <command line>` claims the world refuses it, the recorded refusal containing the text when one is written. |
| `expect` (required) | body | statements | — | one generated `state.world[]` verdict row and one generated `rules[]` entry per line: the rule fires on the last reached tick alone, and its effect writes pass or fail from the expectation beside the values it read | — | What the test claims, one rule-gate expression per line. |
| `world block` | body | statements | — | one generated document per world of the composition, its entry world carrying the `schedule` section whose `instances` arm the rest and whose rows address them by name; a far world's own verdict rule fires one tick before the booted world's export tick, which is where that world stands at the shared export step | — | Inside `given`, `when` or `expect` at the root of a source that emits several worlds: `<world> { … }` addresses one of them by its own name. There is no default world, so every line of such a test stands inside one; `ticks` stays outside, since one grid carries the whole composed run. |

Read back as `test` from the rows it generated when a generated test world is decompiled whose verdict rows, witnesses and verdict rules stand last and whose schedule is seat steps alone: the block prints over the rest of the document, whose boot values already carry what `given` wrote; a composed test's documents arm one another by file and are refused; otherwise a refusal naming the generated row, since printing it as an authored name writes a source the compiler refuses.

### `views`

```puck
views { layout … pipeline … graph … seatRig … seatControl { … } }
```

Printed back as `views`; otherwise the generic value path. Every field of the node the description does not name prints as an ordinary property.

### `workflow`

```puck
workflow name [undo({ rows [row] depth: n })] { step name [skip] { … } … }
```

| Member | Written | Kind | Admitted on | Lowers to | Default | Means |
|---|---|---|---|---|---|---|
| `name` (required) | header | name | — | `name` | — | The group's name, prefixed onto every step it claims. |
| `undo(…)` | modifier | value | — | `undo` | — | The rows and bounded number of completed turns retained for rewindGroup. |
| `steps` (required) | body | statements | — | `steps` | — | The steps the cursor advances through, one per committed firing. |

Printed back as `workflow` when the node carries `name`, `shape`, `steps`, and every rule it claims is in the document's own `rules` array and no other group claims it; otherwise the explicit `ruleGroups` array beside an unclaimed `rules` array. Every field of the node the description does not name prints as an ordinary property.

### `world`

```puck
world name = module(arguments)
```

| Member | Written | Kind | Admitted on | Lowers to | Default | Means |
|---|---|---|---|---|---|---|
| `name` (required) | header | name | — | the output document's identity and file name | — | The output world's name, written as a name or a plain string and never computed, since a reader resolves a document name to the source that emits it from the source's parse alone (`WorldSourceDeclaration`). |
| `module` (required) | header | value | — | the complete generated world document | — | The module invocation expanded with these arguments to produce this world's document. |

Read back as `world` from the rows it generated when the composition's worlds are decompiled together (`WorldDecompiler.DecompileComposition`), each world printing as a module of its own name; otherwise a refusal naming the generated row, since printing it as an authored name writes a source the compiler refuses.

### `request`

```puck
request Capability "subject"
```

| Member | Written | Kind | Admitted on | Lowers to | Default | Means |
|---|---|---|---|---|---|---|
| `capability` (required) | header | name | — | `capability` | — | The capability the addon asks for. |
| `subject` (required) | header | text | — | `subject` | — | What the capability is asked over. |

Printed back as `request` when the node carries `capability`, `subject`; otherwise the generic value path. Every field of the node the description does not name prints as an ordinary property.

### `watchMemory`

```puck
watchMemory screen: n, address: n, length: n
```

| Member | Written | Kind | Admitted on | Lowers to | Default | Means |
|---|---|---|---|---|---|---|
| `screen:` | property | number | — | `screen` | — | The hosted machine's screen index. |
| `address:` | property | number | — | `address` | — | The machine address the watch starts at. |
| `length:` | property | number | — | `length` | — | How many bytes the watch reads. |

Printed back as `watchMemory`; otherwise the generic value path. Every field of the node the description does not name prints as an ordinary property.

### `interrupt`

```puck
interrupt Gate
```

| Member | Written | Kind | Admitted on | Lowers to | Default | Means |
|---|---|---|---|---|---|---|
| `interrupt` (required) | header | gate | — | `interrupt` | — | Reconsiders the choice before the period elapses. |

Printed back as `interrupt` when the node carries `interrupt`, and the gate has a `when` spelling; otherwise `interrupt: <call-form>`, an ordinary decision-body property. Every field of the node the description does not name prints as an ordinary property.

### `onNoChoice`

```puck
onNoChoice { <effect>* }
```

Printed back as `onNoChoice`; otherwise the explicit `onNoChoice` array. Every field of the node the description does not name prints as an ordinary property.

### `option`

```puck
option "name" { [when Gate] score: <operand> [neighbors { … }] <effect>* }
```

| Member | Written | Kind | Admitted on | Lowers to | Default | Means |
|---|---|---|---|---|---|---|
| `name` (required) | header | name | — | `name` | — | The option's name, which the chosen result reports. |
| `score:` (required) | property | operand | — | `score` | — | What the option is worth this period; an option without one is refused. |
| `effects` | body | statements | — | `effects` | — | What fires when this option is the one chosen. |

Printed back as `option` when the node carries `name`, `score`, and its gate has a `when` spelling; otherwise `gate: <call-form>`, an ordinary option-body property. Every field of the node the description does not name prints as an ordinary property.

### `placement`

```puck
placement "id" { prototype: … position: … [solid] }
```

| Member | Written | Kind | Admitted on | Lowers to | Default | Means |
|---|---|---|---|---|---|---|
| `name` (required) | header | name | — | `id` | — | The row's identity, which a child row's `parent` reads it by. |
| `yawDegrees:` | property | fixed | — | `yawDegrees` | `0` | The row's heading about +Y. |
| `scale:` | property | fixed | — | `scale` | `1` | The row's uniform scale. |
| `solid:` | property | flag | — | `solid` | — | Stands for `solid { margin: 0 }`; authoring both the flag and the block is refused. |

Printed back as `placement` when the node carries `id`; otherwise the generic value path, for the whole section. Every field of the node the description does not name prints as an ordinary property.

### `prototype`

```puck
prototype "id" { document { … } }
```

| Member | Written | Kind | Admitted on | Lowers to | Default | Means |
|---|---|---|---|---|---|---|
| `name` (required) | header | name | — | `id` | — | The prototype's identity, which a placement's `prototype` reads it by. |
| `document` (required) | body | statements | — | `document` | — | The creation document itself, an ordinary nested block, so a `shape` inside it reaches the same lowering a root creation document's does. |

Printed back as `prototype` when the node carries `document`, `id`, and its `document` is an object; otherwise the generic value path, for the whole `prototypes` array. Every field of the node the description does not name prints as an ordinary property.

### `claim`

```puck
claim pool as binding { <effect>* }
```

| Member | Written | Kind | Admitted on | Lowers to | Default | Means |
|---|---|---|---|---|---|---|
| `pool` (required) | header | reference | — | `pool` | — | The pool to allocate from. |
| `binding` (required) | header | reference | — | `binding` | — | The lexical name for the claimed instance. |
| `effects` (required) | body | statements | — | `effects` | — | The effects evaluated with the claimed binding in scope. |

Printed back as `claim` when the node carries `binding`, `effects`, `pool`; otherwise the call-form escape hatch. Every field of the node the description does not name prints as an ordinary property.

### `claim pair`

```puck
claim pair pool between left, right as binding { <effect>* }
```

| Member | Written | Kind | Admitted on | Lowers to | Default | Means |
|---|---|---|---|---|---|---|
| `pool` (required) | header | reference | — | `pool` | — | The pair pool to allocate from. |
| `endpoints` (required) | header | reference | — | `left`, `right` | — | The bound instances used as relationship endpoints. |
| `binding` (required) | header | reference | — | `binding` | — | The lexical name for the claimed relationship. |
| `effects` (required) | body | statements | — | `effects` | — | The effects evaluated with the relationship binding in scope. |

Printed back as `claim pair` when the node carries `binding`, `effects`, `pool`; otherwise the call-form escape hatch. Every field of the node the description does not name prints as an ordinary property.

### `deal`

```puck
deal count [from] fromZone [to] toZone
```

| Member | Written | Kind | Admitted on | Lowers to | Default | Means |
|---|---|---|---|---|---|---|
| `count` (required) | header | number | — | `count` | — | How many tokens move in this one transfer, each selected afresh from what remains. |
| `from` (required) | header | reference | — | `from` | — | The ordered zone the tokens leave, taken from its first position. |
| `to` (required) | header | reference | — | `to` | — | The ordered zone the tokens are appended to. |

Printed back as `deal` when the transform is a `transfer` selecting `First` with no key and no `insertFirst`, carrying a count; otherwise `transform transfer(…)`. Every field of the node the description does not name prints as an ordinary property.

One statement writes `rules[].effects[][transformState]`, `rules[].effects[][transformState].transform`.

### `decision`

```puck
decision { periodSeconds: … [mode: …] [interrupt Gate] [onNoChoice { … }] option "name" { … } }
```

| Member | Written | Kind | Admitted on | Lowers to | Default | Means |
|---|---|---|---|---|---|---|
| `periodSeconds:` (required) | property | seconds | — | `periodSeconds` | — | How often the choice is reconsidered. |
| `options` (required) | body | statements | — | `options` | — | The candidates, each with its own gate, score and effects. |

Printed back as `decision` when the node carries `options`, `periodSeconds`; otherwise the explicit `decision` object. Every field of the node the description does not name prints as an ordinary property.

### `draw`

```puck
draw fromZone [to] toZone
```

| Member | Written | Kind | Admitted on | Lowers to | Default | Means |
|---|---|---|---|---|---|---|
| `from` (required) | header | reference | — | `from` | — | The ordered zone the token leaves, taken from its first position. |
| `to` (required) | header | reference | — | `to` | — | The ordered zone the token is appended to. |

Printed back as `draw` when the transform is a `transfer` selecting `First` with no key, no `insertFirst` and no count of its own; otherwise `transform transfer(…)`. Every field of the node the description does not name prints as an ordinary property.

One statement writes `rules[].effects[][transformState]`, `rules[].effects[][transformState].transform`.

### `for each`

```puck
for each binding in pool { <effect>* }
```

| Member | Written | Kind | Admitted on | Lowers to | Default | Means |
|---|---|---|---|---|---|---|
| `pool` (required) | header | reference | — | `pool` | — | The pool whose live instances are visited. |
| `binding` (required) | header | reference | — | `binding` | — | The lexical name for the current instance. |
| `effects` (required) | body | statements | — | `effects` | — | The effects evaluated once for each live instance. |

Printed back as `for each` when the node carries `binding`, `effects`, `pool`; otherwise the call-form escape hatch. Every field of the node the description does not name prints as an ordinary property.

### `if`

```puck
if Gate { <effect>* } [else if Gate { … }]* [else { … }]
```

| Member | Written | Kind | Admitted on | Lowers to | Default | Means |
|---|---|---|---|---|---|---|
| `condition` (required) | header | gate | — | `condition` | — | Read through the same predicate lowering `when` uses. |
| `branches` (required) | body | statements | — | `then`, `else` | — | One nested `if` node per `else if` level, so a chain of any length lowers as a single branch does. |

Printed back as `if` when the node carries `condition`, and the condition has a `when` spelling, since an `if` has no property-style fallback: the parser reads `if(…)` at statement position as this same grammar, never as a generic call; otherwise nothing — decompiling the document fails rather than emitting a source that means something else. Every field of the node the description does not name prints as an ordinary property.

### `local`

```puck
local name = <operand>
```

| Member | Written | Kind | Admitted on | Lowers to | Default | Means |
|---|---|---|---|---|---|---|
| `name` (required) | header | name | — | `name` | — | The local's name, read bare in every operand after it. |
| `expression` (required) | header | operand | — | `expression` | — | What the local evaluates to, once per firing, before any effect runs. |

Printed back as `local` when the node carries `expression`, `name`; otherwise the explicit `locals` array. Every field of the node the description does not name prints as an ordinary property.

### `push`

```puck
push row = <rhs>
```

| Member | Written | Kind | Admitted on | Lowers to | Default | Means |
|---|---|---|---|---|---|---|
| `target` (required) | header | reference | — | `state` | — | The ordered row the value is appended to; the key is minted, so the target carries none. |
| `value` (required) | header | operand | — | `fromState`, `expression` | — | What is appended: a bare row read lowers as `fromState`, anything else as `expression`. |

Printed back as `push` when the node carries `state`, and its target round-trips through `ExpressionSpelling` unchanged; otherwise the call-form escape hatch, `type(k: v, …)`. Every field of the node the description does not name prints as an ordinary property.

### `release`

```puck
release binding
```

| Member | Written | Kind | Admitted on | Lowers to | Default | Means |
|---|---|---|---|---|---|---|
| `binding` (required) | header | reference | — | `binding` | — | The lexical instance binding to release. |

Printed back as `release` when the node carries `binding`; otherwise the call-form escape hatch. Every field of the node the description does not name prints as an ordinary property.

### `remove`

```puck
remove row[key]
```

| Member | Written | Kind | Admitted on | Lowers to | Default | Means |
|---|---|---|---|---|---|---|
| `target` (required) | header | reference | — | `state`, `key` | — | The cell the effect writes, read as one state token. |

Printed back as `remove` when its target round-trips through `ExpressionSpelling` unchanged; otherwise the call-form escape hatch, `type(k: v, …)`. Every field of the node the description does not name prints as an ordinary property.

### `schedule`

```puck
schedule row[key] in Ns
```

| Member | Written | Kind | Admitted on | Lowers to | Default | Means |
|---|---|---|---|---|---|---|
| `target` (required) | header | reference | — | `state`, `key` | — | The cell the effect writes, read as one state token. |
| `in` (required) | header | seconds | — | `delaySeconds` | — | How far ahead the write lands; the time unit is required. |

Printed back as `schedule` when the node carries `delaySeconds`, and its target round-trips through `ExpressionSpelling` unchanged; otherwise the call-form escape hatch, `type(k: v, …)`. Every field of the node the description does not name prints as an ordinary property.

### `shuffle`

```puck
shuffle row [with] drawRow
```

| Member | Written | Kind | Admitted on | Lowers to | Default | Means |
|---|---|---|---|---|---|---|
| `row` (required) | header | reference | — | `row` | — | The ordered zone or keyed row whose cells are reordered in place. |
| `draw` (required) | header | reference | — | `draw` | — | The integer `StreamDraw` row supplying the samples one Fisher-Yates pass consumes. |

Printed back as `shuffle` when the transform is a `shuffle` naming both its row and its draw row; otherwise `transform shuffle(…)`. Every field of the node the description does not name prints as an ordinary property.

One statement writes `rules[].effects[][transformState]`, `rules[].effects[][transformState].transform`.

### `transaction`

```puck
transaction { <effect>* } [onFailure { <effect>* }]
```

| Member | Written | Kind | Admitted on | Lowers to | Default | Means |
|---|---|---|---|---|---|---|
| `effects` (required) | body | statements | — | `effects` | — | The effects that commit together or not at all; a nested transaction is refused. |

Printed back as `transaction` when the node carries `effects`; otherwise the call-form escape hatch. Every field of the node the description does not name prints as an ordinary property.

### `transform`

```puck
transform call(…)
```

| Member | Written | Kind | Admitted on | Lowers to | Default | Means |
|---|---|---|---|---|---|---|
| `transform` (required) | header | value | — | `transform` | — | The `StateTransform` arm, written call-form; its own argument names the destination. |

Printed back as `transform` when the node carries `transform`, and its target round-trips through `ExpressionSpelling` unchanged; otherwise the call-form escape hatch, `type(k: v, …)`. Every field of the node the description does not name prints as an ordinary property.

### `when`

```puck
when Operand cmp Operand [: Int|Fixed] | when Gate and|or Gate | when not Gate | when (Gate)
```

| Member | Written | Kind | Admitted on | Lowers to | Default | Means |
|---|---|---|---|---|---|---|
| `gate` (required) | header | gate | — | `gate` | — | The predicate; a second `when` in one rule or option is refused. |

Printed back as `when` when the node carries `gate`, and no `compareValue` in it opens its left operand with `(` and no `all`/`any` in it carries fewer than two children; otherwise `gate: <call-form>`, an ordinary rule-body property. Every field of the node the description does not name prints as an ordinary property.

### `space`

```puck
space name { model: … revision: … dimensions: … }
```

| Member | Written | Kind | Admitted on | Lowers to | Default | Means |
|---|---|---|---|---|---|---|
| `name` (required) | header | name | — | `name` | — | The space's name, which a Vector row's `space(…)` modifier names. |
| `model:` (required) | property | text | — | `model` | — | The embedding model the lock file beside the source was produced with. |
| `revision:` (required) | property | text | — | `revision` | — | The model revision, so a lock produced against another one is refused rather than read. |
| `dimensions:` (required) | property | number | — | `dimensions` | — | The vector width, inside 8 to 1024. |

Printed back as `space` when the node carries `dimensions`, `model`, `name`, `revision`; otherwise the generic value path. Every field of the node the description does not name prints as an ordinary property.

### `spaces`

```puck
spaces { space name { … } }
```

Printed back as `spaces`; otherwise the generic value path. Every field of the node the description does not name prints as an ordinary property.

### `world`

```puck
world { table | slot | pile | grid | row … }
```

Printed back as `world` when every row in it sugars or falls back to `row { }` on its own; otherwise the explicit `world [ ]` array. Every field of the node the description does not name prints as an ordinary property.

### `onFailure`

```puck
onFailure { <effect>* }
```

Printed back as `onFailure`; otherwise the call-form escape hatch. Every field of the node the description does not name prints as an ordinary property.

### `graph`

```puck
graph "name" { … }
```

| Member | Written | Kind | Admitted on | Lowers to | Default | Means |
|---|---|---|---|---|---|---|
| `name` (required) | header | name | — | `name` | — | The instance's name, which a layout slot, another instance's input and the pipeline verbs name. |

Printed back as `graph` when the node carries `name`; otherwise the generic value path. Every field of the node the description does not name prints as an ordinary property.

### `layout`

```puck
layout "name" { … }
```

| Member | Written | Kind | Admitted on | Lowers to | Default | Means |
|---|---|---|---|---|---|---|
| `name` (required) | header | name | — | `name` | — | The layout's name, which a session selects it by. |

Printed back as `layout` when the node carries `name`; otherwise the generic value path. Every field of the node the description does not name prints as an ordinary property.

### `pipeline`

```puck
pipeline "name" { … }
```

| Member | Written | Kind | Admitted on | Lowers to | Default | Means |
|---|---|---|---|---|---|---|
| `name` (required) | header | name | — | `name` | — | The pipeline's name, which a layout slot names it by. |

Printed back as `pipeline` when the node carries `name`; otherwise the generic value path. Every field of the node the description does not name prints as an ordinary property.

### `seatControl`

```puck
seatControl { … }
```

Printed back as `seatControl`; otherwise the generic value path. Every field of the node the description does not name prints as an ordinary property.

### `seatRig`

```puck
seatRig "name" { version: … operations [ … ] }
```

| Member | Written | Kind | Admitted on | Lowers to | Default | Means |
|---|---|---|---|---|---|---|
| `name` (required) | header | name | — | `name` | — | The rig's name. |
| `operations:` | property | value | — | `operations` | — | The camera program, each operation written call-form. |

Printed back as `seatRig` when the node carries `name`; otherwise the generic value path. Every field of the node the description does not name prints as an ordinary property.

### `step`

```puck
step name [skip] { [when Gate] <effect>* }
```

| Member | Written | Kind | Admitted on | Lowers to | Default | Means |
|---|---|---|---|---|---|---|
| `name` (required) | header | name | — | `name` | — | The step's name, which the group prefixes. |
| `skip` | header | flag | — | `onRefusal` | — | Advances the cursor past this step's own refusal instead of stalling on it. |

Printed back as `step` when the node carries `name`; otherwise one `rule` block, printed outside the group. Every field of the node the description does not name prints as an ordinary property.

One statement writes `rules[]`, `ruleGroups[].steps[]`.

### `grid`

```puck
grid name[family] [: Enum] dimensions(width:, depth:) [wrap(…)] [cellSize(n)] [origin(x, y, z)] [band(n)] [empty(v)] [positions(row)] [inverse(tokens:, codes:)] [bounds(…)] [{ "ordinal" = value … }]
```

| Member | Written | Kind | Admitted on | Lowers to | Default | Means |
|---|---|---|---|---|---|---|
| `name` (required) | header | name | — | `name` | — | The row's name, which is also the name of the topology the declaration mints. |
| `enum` | header | reference | — | `enum` | — | The declared enum the row's cells are drawn from, written `: Enum` after the name; the row is then Int. |
| `dimensions(…)` (required) | modifier | value | — | `width`, `depth` | — | The topology's cell counts along +X and +Z. |
| `wrap(…)` | modifier | enumeration (`None`, `X`, `Y`, `Both`) | — | `wrap` | `"None"` | The topology's wrapped axes. |
| `cellSize(…)` | modifier | length | — | `cellSize` | `1` | The topology's cubic cell edge, in world units. |
| `origin(…)` | modifier | point | — | `origin` | `[0, 0, 0]` | The topology's minimum corner, in world units. |
| `band(…)` | modifier | length | — | `band` | `0` | The vertical half-extent a position must lie within to resolve to a cell; 0 admits any height. |
| `empty(…)` | modifier | value | — | `domain` | `0` | The value an unwritten cell reads, a raw integer on the wire whatever the row's kind. |
| `positions(…)` | modifier | reference | — | `valuesFrom` | — | Marks another row's integer values as this topology's cell ordinals, by setting that row's `valuesFrom`. |
| `inverse(…)` | modifier | value | — | `inverse` | — | Derives the board from a tokens row and a codes row instead of from an authored body. |
| `bounds(…)` | modifier | value | `Int` | `min`, `max`, `overflow` | — | The row's range and what a write past it does; only where the row's kind is Int. |
| `cells` | body | cells | — | `cells` | — | Initial cells keyed by literal topology ordinal; refused beside `inverse`. |

Printed back as `grid` when the node carries `name`, and it carries no key outside `cells`, `domain`, `enum`, `inverse`, `kind`, `max`, `min`, `name`, `overflow`, and it is the only row over its topology, and that topology carries nothing outside what the modifiers can spell; otherwise `row { }` beside an explicit `state.lattices` entry.

One statement writes `state.world[]`, `state.lattices[]`.

### `pile`

```puck
pile name[family] of tokenRow [capacity(n)] { token … }
```

| Member | Written | Kind | Admitted on | Lowers to | Default | Means |
|---|---|---|---|---|---|---|
| `name` (required) | header | name | — | `name` | — | The row's name, which every rule and binding reads it by. |
| `of` (required) | header | reference | — | `domain` | — | The plain token-domain row supplying the keys this pile holds membership over. |
| `capacity(…)` | modifier | number | — | `capacity` | — | The pile's member ceiling, never past its token domain's own. |
| `tokens` | body | tokens | — | `cells` | — | The tokens the pile starts holding, in written order; a repeat is refused. |

Printed back as `pile` when the node carries `domain`, `name`, and it carries no key outside `capacity`, `cells`, `domain`, `kind`, `name`, and its kind is Bool, its domain is the ordered `keysOf` shape with nothing else on it, and every cell is a bare key holding `true`; otherwise `row { }`, the explicit form.

### `row`

```puck
row { <every explicit StateRow field> }
```

Printed back as `row`; otherwise nothing — a `row { }` is what the four declarations fall back to. Every field of the node the description does not name prints as an ordinary property.

### `slot`

```puck
slot name[family] [: Enum] [= value] [bounds([minimum]..[maximum], overflow:)] [advance(perSecond:)] [space(name)]
```

| Member | Written | Kind | Admitted on | Lowers to | Default | Means |
|---|---|---|---|---|---|---|
| `name` (required) | header | name | — | `name` | — | The row's name, which every rule and binding reads it by. |
| `enum` | header | reference | — | `enum` | — | The declared enum the row's cells are drawn from, written `: Enum` after the name; the row is then Int. |
| `value` | header | value | — | `value` | — | The slot's initial value; omitted, the row gains its cell from the first write. |
| `bounds(…)` | modifier | value | `Int`, `Fixed` | `min`, `max`, `overflow` | — | The row's range and what a write past it does; only where the row's kind is Int or Fixed. |
| `advance(…)` | modifier | rate | `Int`, `Fixed` | `advance` | — | Per-second continuous accumulation, its sign carrying the direction; only where the row's kind is Int or Fixed. |
| `space(…)` | modifier | reference | `Vector` | `space` | — | The declared embedding space the row's value lives in; only where the row's kind is Vector. |

Printed back as `slot` when the node carries `name`, and it carries no key outside `advance`, `enum`, `kind`, `max`, `min`, `name`, `overflow`, `space`, `value`, and its name is a bare identifier, its kind is a cell kind, it carries no `domain`, and every bound and its value is a literal of that kind; otherwise `row { }`, the explicit form.

### `table`

```puck
table name[family] [: Enum] [capacity(n)] [bounds([minimum]..[maximum], overflow:)] [advance(perSecond:)] [evicts] [space(name)] [embeds(row)] { key = value [advance(perSecond:)] [behavior(none)] }
```

| Member | Written | Kind | Admitted on | Lowers to | Default | Means |
|---|---|---|---|---|---|---|
| `name` (required) | header | name | — | `name` | — | The row's name, which every rule and binding reads it by. |
| `enum` | header | reference | — | `enum` | — | The declared enum the row's cells are drawn from, written `: Enum` after the name; the row is then Int. |
| `capacity(…)` | modifier | number | — | `capacity` | — | The row's cell-count ceiling; smaller than its own authored cells is refused. |
| `bounds(…)` | modifier | value | `Int`, `Fixed` | `min`, `max`, `overflow` | — | The row's range and what a write past it does; only where the row's kind is Int or Fixed. |
| `advance(…)` | modifier | rate | `Int`, `Fixed` | `advance` | — | Per-second continuous accumulation, its sign carrying the direction; only where the row's kind is Int or Fixed. |
| `evicts(…)` | modifier | flag | — | `evicts` | — | Drops the oldest cell instead of refusing a write at capacity. |
| `space(…)` | modifier | reference | `Vector` | `space` | — | The declared embedding space the row's cells live in; only where the row's kind is Vector. |
| `embeds(…)` | modifier | reference | `Text` | mints a companion `state.world[]` Vector row | — | Mints a companion Vector row over this row's own keys; only where the row's kind is Text. |
| `cells` | body | cells | — | `cells` | — | The row's initial cells, omitted from the document when the table has none. |
| `key` (required) | cell | name | — | `key` | — | One cell's key; the reserved `$` prefix is refused. |
| `value` (required) | cell | value | — | `value` | — | The cell's initial value, converted per the row's kind. |
| `advance` | cell | rate | `Int`, `Fixed` | `advance` | — | This cell's own accumulation rate, in place of the row's; only where the row's kind is Int or Fixed. |
| `behavior` | cell | enumeration (`none`) | — | `behavior` | — | Opts the cell out of the row's accumulation; refused beside the cell's own `advance`. |

Printed back as `table` when the node carries `name`, and it carries no key outside `advance`, `capacity`, `cells`, `domain`, `enum`, `evicts`, `kind`, `max`, `min`, `name`, `overflow`, `space`, and its name is a bare identifier, its kind is a cell kind, its `domain` is the bare `keys` shape the lowering would itself have written, every bound and cell value is a literal of the row's kind, and every `advance` rate reduces to one literal; otherwise `row { }`, the explicit form.

One body entry carries `advance`, `behavior`, `key`, `value` and nothing else.

## Not described, and why

| Construct | Why |
|---|---|
| `+=` | An assignment, so there is no keyword for a reader, a diagnostic or this table to key on; lowers to `addState`. |
| `=` | An assignment, so there is no keyword for a reader, a diagnostic or this table to key on; lowers to `setState`. |
| `basis` | Core grammar: a document header this vocabulary does not interpret. |
| `export` | Core grammar: the module surface, described by `Puck.Transpiler`. |
| `for` | Core grammar: the compile-time layer, evaluated away before this vocabulary sees a statement. |
| `import` | Core grammar: the module surface, described by `Puck.Transpiler`. |
| `let` | Core grammar: the compile-time layer, evaluated away before this vocabulary sees a statement. |
| `module` | Core grammar: a typed compile-time module, expanded before this vocabulary sees a statement. |
| `schema` | Core grammar: a document header this vocabulary does not interpret. |
| `template` | Core grammar: the compile-time layer, evaluated away before this vocabulary sees a statement. |
| `use` | Core grammar: a module instantiation, expanded before this vocabulary sees a statement. |

---

[Reference](README.md) · [The language core](dsl.md) ·
[The world vocabulary](../../src/Puck.World.Transpiler/README.md)

{
    "$schema": "../../schema/run.schema.json",
    "version": "puck.run.v1",
    "_comment": "Corpus entry for the fuzzing document section (parse/validation coverage via Puck.Post's run-document stage). The live differential sweep is `tools fuzz`, which spawns `Puck.Post --stage fuzz --fuzz-seed N` per seed; the demo's document-driven fuzz path was deleted in the 2026-07-03 purification.",
    "fuzzing": {
        "seedRange": [0, 2],
        "timeoutSeconds": 30,
        "bounds": { "maxPrimitives": 8, "maxMaterials": 4 }
    }
}

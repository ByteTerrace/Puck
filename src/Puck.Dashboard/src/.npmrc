# Remote tarball dependencies are allowed only where a package.json in this workspace names one directly (the Puck
# build of DuckDB-Wasm, see ../README.md); npm 12 refuses them by default, and none may arrive transitively.
allow-remote=root

namespace Puck.SignedDistance;

/// <summary>Defines the per-screen text-decal cell budget the flat-coverage <c>ScreenSlab</c> decal tier packs
/// against.</summary>
public static class SdfScreenDecalLayout {
    /// <summary>The per-screen decal cell budget — e.g. up to a 48×32 or 53×30 terminal grid.</summary>
    public const int MaxScreenDecalCells = 1600;
}

# Border-margin strip canary — the neighbour's ground is really under the body.
#
# Run: dotnet run --project src/Puck.World -c Release -- --world src/Puck.World/Assets/worlds/quilt-nw-gap.world.json --exit-after-seconds 12 --state-dir <fresh-tmp-dir> < this-file
#
# Proves: a body standing where its OWN world has no floor is held up by the NEIGHBOUR's
# ground, delivered through the border isometry. quilt-nw-gap authors a notch subtracted from
# quilt-nw's own floor beside door-to-ne, whose near edge sits at the authored marginDepth of
# 1.2 — so nothing of quilt-nw's own geometry can support a body at x = -0.7, and quilt-nw's
# own plane does not resume until y = -1.0.
#
# Expected: pos = (-0.70, 0.02, -12.00), grounded, wire.errors 0.
#
# The discriminating control, which is why this file is a canary rather than a smoke test:
# comment out the BorderMargin assignment in WorldPostBuildWiring (the one line that hands
# IWorldBorderMarginSource to WorldServer), rebuild, and re-run. The body then falls through to
# quilt-nw's own plane and reports y = -0.98. Same document, same pose, one variable, opposite
# outcomes. A run that reports -0.98 with the assignment intact means the strip has stopped
# engaging — which it has done before, silently, while its own law stayed green.
#
# Why the law is not enough on its own: BorderMarginRuntimeLawTests once passed against a
# body it constructed itself, below the composition root, with the source injected before the
# seat joined and a synthetic face oriented so the band's half-space test agreed with its own
# error. Both mismatches were invisible to it and fatal in production. This file exercises the
# real loader, the real composition root, the real boot order and the real authored geometry,
# so it can only pass for the right reason.

player.pose -0.7 2 -12 0 0 0 1
world.wait 180
player.where 1
world.contacts
wire.errors

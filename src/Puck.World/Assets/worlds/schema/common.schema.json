{
  "$defs": {
    "WorldRenderScaleTier": {
      "description": "The SAFE, enumerated world render-scale tiers a run pins for the settled REVEALED room view \u2014 the demo/user-facing quality option layered over the engine\u0027s CONTINUOUS render-scale knob (SdfViewSnapshot.RenderScale, quantized to SdfWorldEngine.RenderScaleQ). The player picks one of these KNOWN-GOOD steps, never a free numeric value; each tier is pinned to an exact quantized numerator so the reduced render extent (worldRenderDims) is a stable integer at any window size and the bilinear upsample stays artifact-free. The continuous knob stays reachable PROGRAMMATICALLY (layout eases, dev tooling) \u2014 the enumerated policy lives only here at the user surface. Names \u002B scales are the ONE definition (WorldRenderScaleTiers); the world document\u0027s quality presets, the console world.render-scale verb, and the boot resolution all read them there.",
      "enum": [
        "Native",
        "ThreeQuarter",
        "Half",
        "Quarter",
        "Eighth"
      ]
    },
    "WorldQualityPreset": {
      "type": [
        "object",
        "null"
      ],
      "properties": {
        "shadows": {
          "description": "The soft-shadow tier the preset selects.",
          "enum": [
            "Off",
            "Low",
            "Medium",
            "High"
          ]
        },
        "ambientOcclusion": {
          "description": "Whether the preset enables ambient occlusion.",
          "type": "boolean"
        },
        "renderScale": {
          "$ref": "#/$defs/WorldRenderScaleTier",
          "description": "The render-scale tier the preset selects."
        }
      },
      "additionalProperties": false
    },
    "WorldRenderSun": {
      "description": "One directional sun. Every field is optional individually \u2014 absent resolves to SdfFrame\u0027s pinned default for that field.",
      "type": [
        "object",
        "null"
      ],
      "properties": {
        "direction": {
          "description": "The direction from a lit surface toward the light, any nonzero length (normalized host-side before upload).",
          "default": null
        },
        "weight": {
          "description": "The sun\u0027s diffuse weight.",
          "type": [
            "number",
            "null"
          ],
          "default": null
        },
        "color": {
          "description": "The sun\u0027s linear #RRGGBB color.",
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      },
      "additionalProperties": false
    },
    "WorldRenderAmbient": {
      "description": "The ambient (hemisphere) term. Every field is optional individually \u2014 absent resolves to SdfFrame\u0027s pinned default for that field.",
      "type": [
        "object",
        "null"
      ],
      "properties": {
        "base": {
          "description": "The ambient floor.",
          "type": [
            "number",
            "null"
          ],
          "default": null
        },
        "hemisphere": {
          "description": "The hemisphere gradient, scaling surface normal Y.",
          "type": [
            "number",
            "null"
          ],
          "default": null
        },
        "color": {
          "description": "The ambient linear #RRGGBB color.",
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      },
      "additionalProperties": false
    },
    "WorldRenderLighting": {
      "description": "The scene\u0027s directional sun and ambient term \u2014 threads unchanged into the engine\u0027s existing per-frame lighting fields (SdfFrame.SunDirection/SunWeight/SunColor/AmbientBase/ AmbientHemisphere/AmbientColor). Every field, at every level, is optional individually \u2014 an absent one resolves to SdfFrame\u0027s pinned default for that field, so an unauthored section, or a partially authored one, renders exactly as a world that declares neither.",
      "type": [
        "object",
        "null"
      ],
      "properties": {
        "sun": {
          "$ref": "#/$defs/WorldRenderSun",
          "description": "The directional sun.",
          "default": null
        },
        "ambient": {
          "$ref": "#/$defs/WorldRenderAmbient",
          "description": "The ambient (hemisphere) term.",
          "default": null
        }
      },
      "additionalProperties": false
    },
    "WorldRenderSkySun": {
      "description": "The visible sun disc \u2014 an additive highlight about the lighting sun\u0027s direction.",
      "type": [
        "object",
        "null"
      ],
      "properties": {
        "discRadians": {
          "description": "The disc\u0027s angular half-radius in radians, in (0, \u03C0/2]. Controls the falloff sharpness: a smaller disc reads sharper.",
          "type": "number"
        },
        "intensity": {
          "description": "The disc\u0027s peak additive brightness.",
          "type": "number"
        }
      },
      "required": [
        "discRadians",
        "intensity"
      ],
      "additionalProperties": false
    },
    "WorldRenderSkyTwinkle": {
      "description": "Scintillation: a hash-chosen share of the stars dip and recover on the simulation clock, each at its own harmonic and phase of one authored rate, so no two twinkle in step. Presentation-only, keyed on the tick.",
      "type": [
        "object",
        "null"
      ],
      "properties": {
        "share": {
          "description": "The fraction of stars that twinkle, in [0, 1]. Zero twinkles none.",
          "type": "number"
        },
        "depth": {
          "description": "How far a twinkling star dips below its steady brightness, in [0, 1]: 0 holds steady, 1 dips to black.",
          "type": "number"
        },
        "rate": {
          "description": "The fundamental scintillation rate in hertz; each star twinkles at a small harmonic of it.",
          "type": "number"
        }
      },
      "required": [
        "share",
        "depth",
        "rate"
      ],
      "additionalProperties": false
    },
    "WorldRenderSkyStars": {
      "description": "The procedural star field: a deterministic per-cell hash over an octahedral sky projection. No texture, no session state \u2014 the identical density/seed always draws the identical field.",
      "type": [
        "object",
        "null"
      ],
      "properties": {
        "density": {
          "description": "The star grid\u0027s cell count per octahedral axis. A higher value packs more, smaller stars.",
          "type": "number"
        },
        "brightness": {
          "description": "The peak per-star brightness.",
          "type": "number"
        },
        "seed": {
          "description": "The hash seed folded into every cell \u2014 a different seed reshuffles the field.",
          "type": "integer"
        },
        "twinkle": {
          "$ref": "#/$defs/WorldRenderSkyTwinkle",
          "description": "Scintillation for a share of the stars. Optional; absent twinkles none.",
          "default": null
        }
      },
      "required": [
        "density",
        "brightness",
        "seed"
      ],
      "additionalProperties": false
    },
    "WorldRenderSkyClouds": {
      "description": "The procedural cloud layer: a deterministic hashed-lattice noise (warped, four octaves) on a plane above the camera, thresholded by coverage, drawn over the gradient, sun disc and stars and fading into the horizon. No texture, no session state \u2014 the identical settings and seed always draw the identical layer at the identical tick.",
      "type": [
        "object",
        "null"
      ],
      "properties": {
        "coverage": {
          "description": "The fraction of the sky the layer covers, in [0, 1]. Zero draws none.",
          "type": "number"
        },
        "softness": {
          "description": "The width of a cloud\u0027s edge, in (0, 1]: small is hard-edged cumulus, large is a diffuse haze.",
          "type": "number"
        },
        "scale": {
          "description": "The size of one cloud cell in layer units (the layer sits at unit height, so a scale of 1 spans about 45\u00B0 overhead). Larger is broader clouds.",
          "type": "number"
        },
        "seed": {
          "description": "The hash seed folded into the lattice \u2014 a different seed reshapes the layer.",
          "type": "integer"
        },
        "color": {
          "description": "The cloud colour, as #RRGGBB or a state.\u003Crow\u003E.\u003Ckey\u003E binding. Optional; absent is white.",
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "drift": {
          "description": "The layer\u0027s wind, in layer units per second along world X and Z, integrated on the tick clock. Optional; absent holds still.",
          "default": null
        },
        "spin": {
          "description": "The layer\u0027s rotation about the zenith in radians per second \u2014 the planetary-scale turning a rotating frame gives a broad flow; positive is counter-clockwise seen from below. Optional; absent is none.",
          "type": [
            "number",
            "null"
          ],
          "default": null
        },
        "curl": {
          "description": "The Coriolis twist: how far, in radians, the layer is wound about the zenith at 45\u00B0 elevation, the winding falling off toward the horizon and the zenith so bands spiral in rather than shear apart. Positive winds counter-clockwise. Optional; absent is none.",
          "type": [
            "number",
            "null"
          ],
          "default": null
        },
        "shear": {
          "description": "The wind of the SHAPING field relative to the cloud field, in layer units per second: the two slide past each other, so clouds boil and re-form as they drift rather than glide as a fixed picture. Optional; absent holds their shapes.",
          "default": null
        }
      },
      "required": [
        "coverage",
        "softness",
        "scale",
        "seed"
      ],
      "additionalProperties": false
    },
    "WorldRenderSky": {
      "description": "The procedural sky \u2014 a three-stop gradient, sun disc, star field, and distance fog, authored as world data. Absent is a hard gate: every existing world renders the pinned two-stop gradient and 0.015 fog density bit-exactly, as before this section existed, until it authors one.",
      "type": [
        "object",
        "null"
      ],
      "properties": {
        "zenith": {
          "description": "The straight-up sky color, as #RRGGBB. Optional; absent takes the pinned zenith.",
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "horizon": {
          "description": "The horizon-band color (the gradient\u0027s middle stop), as #RRGGBB. Optional; absent takes the midpoint between the pinned ground and zenith.",
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "ground": {
          "description": "The straight-down (nadir) color, as #RRGGBB. Optional; absent takes the pinned ground.",
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "fogDensity": {
          "description": "The exponential distance-fog density fading toward the sky color. Optional; absent takes the pinned 0.015 \u2014 the exact value the fog term used before this field existed.",
          "type": [
            "number",
            "null"
          ],
          "default": null
        },
        "sun": {
          "$ref": "#/$defs/WorldRenderSkySun",
          "description": "The visible sun disc, drawn about the lighting sun\u0027s direction. Optional; absent draws no disc.",
          "default": null
        },
        "stars": {
          "$ref": "#/$defs/WorldRenderSkyStars",
          "description": "The procedural star field. Optional; absent draws no stars.",
          "default": null
        },
        "clouds": {
          "$ref": "#/$defs/WorldRenderSkyClouds",
          "description": "The procedural cloud layer. Optional; absent draws no clouds.",
          "default": null
        }
      },
      "additionalProperties": false
    },
    "WorldScreenSourceNone": {
      "description": "No provider is bound \u2014 the engine lights the slot with its procedural no-signal fallback (an animated test-card / striped no-signal look, never black).",
      "properties": {
        "$type": {
          "const": "none"
        }
      },
      "additionalProperties": false
    },
    "WorldScreenSourceTestPattern": {
      "description": "The deterministic animated test pattern (Puck.SdfVm.Views.TestPatternSource), rendered from the world\u0027s sim tick (never the wall clock) into a CPU buffer and uploaded each frame.",
      "properties": {
        "$type": {
          "const": "testPattern"
        },
        "width": {
          "description": "The pattern framebuffer width in pixels.",
          "type": "integer"
        },
        "height": {
          "description": "The pattern framebuffer height in pixels.",
          "type": "integer"
        }
      },
      "required": [
        "width",
        "height"
      ],
      "additionalProperties": false
    },
    "WorldMachineCable": {
      "description": "One machine\u0027s cable-port declaration \u2014 the machine-tier home of cable linking. A cable is the SET of declared machine sources naming the same cable name, derived by MachineCableGroups; no port ever points at another port, so reciprocity holds by construction and a one-port cable is a validation refusal rather than a dangling half-pair.",
      "type": [
        "object",
        "null"
      ],
      "properties": {
        "name": {
          "description": "The cable\u0027s stable kebab-case name, shared by every plugged port.",
          "type": "string"
        },
        "position": {
          "description": "This machine\u0027s 0-based place in cable order \u2014 contiguous across the cable\u0027s ports (validated), and what decides the linking engine\u0027s deterministic player order.",
          "type": "integer"
        }
      },
      "required": [
        "name",
        "position"
      ],
      "additionalProperties": false
    },
    "WorldScreenSourceMachine": {
      "description": "An arbitrary deterministic machine\u0027s unresampled framebuffer \u2014 resolved against a registered IScreenMachineEngine by Engine id. The world never names a concrete machine: the engine owns its Options vocabulary (a GamingBrick reads a dmg/cgb/agb model \u002B a dmgspeed pin).",
      "properties": {
        "$type": {
          "const": "machine"
        },
        "engine": {
          "description": "The screen-machine engine id (e.g. gaming-brick).",
          "type": "string"
        },
        "contentPath": {
          "description": "The content file (a cartridge ROM) the machine boots, or empty when the screen is unconfigured \u2014 the binder faults the slot gracefully (no crash, no-signal card) rather than booting.",
          "type": "string"
        },
        "options": {
          "description": "The engine-specific options string, or null for the engine\u0027s defaults.",
          "type": [
            "string",
            "null"
          ]
        },
        "cable": {
          "$ref": "#/$defs/WorldMachineCable",
          "description": "This machine\u0027s cable port (see WorldMachineCable), or null for an unlinked machine. Legal only on a declared screens row\u0027s own source \u2014 a magazine entry or a placement face\u0027s source is refused one (validated), because a cable is a standing physical connection of the machine that owns the slot, not of whatever content happens to rotate through it. Omitted from the wire when null, so every machine source authored before cables existed round-trips unchanged.",
          "default": null
        }
      },
      "required": [
        "engine",
        "contentPath",
        "options"
      ],
      "additionalProperties": false
    },
    "WorldFeedProfile": {
      "type": [
        "object",
        "null"
      ],
      "properties": {
        "width": {
          "description": "Requested output width in pixels.",
          "type": "integer"
        },
        "height": {
          "description": "Requested output height in pixels.",
          "type": "integer"
        },
        "refreshRateHz": {
          "description": "Maximum pull/upload cadence; it must divide the engine time base exactly.",
          "type": "integer"
        }
      },
      "additionalProperties": false
    },
    "WorldCameraVendorControl": {
      "description": "One raw vendor-extension write on the shared camera (see WorldCameraControls\u0027s Vendor member): a byte-sized selector on the device\u0027s vendor unit and the value to write. Deliberately semantics-free \u2014 the honest vocabulary for device controls no standard names.",
      "type": [
        "object",
        "null"
      ],
      "properties": {
        "id": {
          "description": "The vendor extension unit\u0027s control selector.",
          "type": "integer"
        },
        "value": {
          "description": "The byte value to write (0..255; the device clamps or refuses out-of-range writes).",
          "type": "integer"
        }
      },
      "required": [
        "id",
        "value"
      ],
      "additionalProperties": false
    },
    "WorldCameraControls": {
      "description": "The authored control state for the shared physical camera \u2014 one device-wide state across its color and infrared streams \u2014 and the standard UVC camera/image controls (pan/tilt/zoom, exposure, focus, color). Every member is optional: an ABSENT member leaves that control at the device\u0027s own driver default (automatic where the device supports it), and a PRESENT member drives the control manually at that value. The device remains authoritative \u2014 a value outside the device\u0027s reported range is clamped (and step-snapped) at apply, a control the device lacks is skipped, and screen.camera reads the resulting live state (each control\u0027s device range, mode, and current value) back over the pipe. Removing a previously authored member restores that control\u0027s driver default on the next apply. Ranges are device-specific by design, so validation admits any integer rather than guessing one device\u0027s envelope.",
      "type": [
        "object",
        "null"
      ],
      "properties": {
        "pan": {
          "description": "Horizontal framing offset (digital pan on webcams).",
          "type": [
            "integer",
            "null"
          ],
          "default": null
        },
        "tilt": {
          "description": "Vertical framing offset (digital tilt on webcams).",
          "type": [
            "integer",
            "null"
          ],
          "default": null
        },
        "zoom": {
          "description": "Magnification \u2014 on sensor-cropping webcams a region-of-interest zoom (e.g. 100..500 = 1x..5x on a Logitech BRIO), which pairs with Pan/Tilt to frame a region.",
          "type": [
            "integer",
            "null"
          ],
          "default": null
        },
        "exposure": {
          "description": "Manual exposure time, typically log2 seconds (e.g. -5 = 1/32 s); absent = auto exposure.",
          "type": [
            "integer",
            "null"
          ],
          "default": null
        },
        "focus": {
          "description": "Manual focus distance; absent = autofocus.",
          "type": [
            "integer",
            "null"
          ],
          "default": null
        },
        "brightness": {
          "description": "Image brightness offset.",
          "type": [
            "integer",
            "null"
          ],
          "default": null
        },
        "contrast": {
          "description": "Image contrast.",
          "type": [
            "integer",
            "null"
          ],
          "default": null
        },
        "saturation": {
          "description": "Color saturation (0 is grayscale on most devices).",
          "type": [
            "integer",
            "null"
          ],
          "default": null
        },
        "sharpness": {
          "description": "Edge sharpening strength.",
          "type": [
            "integer",
            "null"
          ],
          "default": null
        },
        "gain": {
          "description": "Sensor gain (ISO-like amplification).",
          "type": [
            "integer",
            "null"
          ],
          "default": null
        },
        "whiteBalance": {
          "description": "Manual white-balance color temperature in kelvin; absent = auto white balance.",
          "type": [
            "integer",
            "null"
          ],
          "default": null
        },
        "backlightCompensation": {
          "description": "Backlight compensation (devices commonly report a 0..1 toggle).",
          "type": [
            "integer",
            "null"
          ],
          "default": null
        },
        "fieldOfView": {
          "description": "The lens field of view in DEGREES \u2014 a vendor-extension control (hardware-verified on the Logitech BRIO: discrete 90/78/65, a true sensor crop composing with Zoom); the device snaps to its nearest supported value, and a camera without the vendor unit skips it. Absent = the device default (widest).",
          "type": [
            "integer",
            "null"
          ],
          "default": null
        },
        "vendor": {
          "description": "Raw vendor-extension writes \u2014 byte-sized (selector, value) pairs on the device\u0027s vendor unit, applied in order after the named controls. The engine assigns NO semantics: the author names the selector, the device decides what it means (e.g. the BRIO\u0027s unconfirmed HDR candidate is selector 12), and a removed row is NOT restored (its default is unknowable). screen.camera reads each authored selector back. Omitted from the wire when null.",
          "type": [
            "array",
            "null"
          ],
          "items": {
            "$ref": "#/$defs/WorldCameraVendorControl"
          },
          "default": null
        }
      },
      "additionalProperties": false
    },
    "WorldScreenSourceCamera": {
      "description": "The platform\u0027s default live camera feed. The platform may negotiate a nearby extent; every screen and probe socket naming the same sensor shares one feed, opened at the richest profile any screen row requests.",
      "properties": {
        "$type": {
          "const": "camera"
        },
        "profile": {
          "$ref": "#/$defs/WorldFeedProfile",
          "description": "The preferred capture extent and maximum upload cadence, or null for the platform default \u2014 a probe socket never needs one. Omitted from the wire when null.",
          "default": null
        },
        "controls": {
          "$ref": "#/$defs/WorldCameraControls",
          "description": "The authored device-control state (WorldCameraControls), or null to leave every control at its driver default. One physical device carries one control state across color and infrared, so the FIRST declared camera screen authoring this wins regardless of sensor (matching the shared-device model); a later UpsertScreen mutation re-resolves and applies the change live. Omitted from the wire when null.",
          "default": null
        },
        "sensor": {
          "description": "Which physical sensor this row\u0027s shared feed opens: Color (the default) or Infrared \u2014 the infrared frame source a Windows Hello capable device carries. Each sensor gets its own shared feed, so different rows may request different sensors at once; the engine honors a Windows Face Authentication Profile V2 when published and admits simultaneous capture only after both native streams prove live. A legacy provider available only to the Windows biometric broker is not a public dual-camera graph. An absent infrared source faults the bind loudly (the slot shows the no-signal card).",
          "enum": [
            "Color",
            "Infrared"
          ],
          "default": "Color"
        },
        "seat": {
          "description": "The 1-based local seat this row names \u2014 a camera is an input device seated like a pad, never hardware named directly. null means the enclosing seat scope (an identity\u0027s HUD panel, a seat-scoped probe socket) or seat 1 at world scope \u2014 the same explicit-index convention $channel:\u003Cseat\u003E and axis bindings already use. Validated within 1..population.localSeats when present. Omitted from the wire when null.",
          "type": [
            "integer",
            "null"
          ],
          "default": null
        }
      },
      "additionalProperties": false
    },
    "WorldScreenSourceView": {
      "description": "A named view from the presentation view stack, such as a monitor showing another camera\u0027s output.",
      "properties": {
        "$type": {
          "const": "view"
        },
        "cameraName": {
          "description": "The registered view name this slot samples.",
          "type": "string"
        }
      },
      "required": [
        "cameraName"
      ],
      "additionalProperties": false
    },
    "WorldFeedProfileNonNullable": {
      "description": "A live screen feed\u0027s requested output policy. It belongs to the source declaration rather than the binder, so two window captures can choose different extents and cadences. Camera extents are preferences because a physical device remains authoritative for its negotiated format.",
      "type": "object",
      "properties": {
        "width": {
          "description": "Requested output width in pixels.",
          "type": "integer"
        },
        "height": {
          "description": "Requested output height in pixels.",
          "type": "integer"
        },
        "refreshRateHz": {
          "description": "Maximum pull/upload cadence; it must divide the engine time base exactly.",
          "type": "integer"
        }
      },
      "additionalProperties": false
    },
    "WorldScreenSourceCapture": {
      "description": "A live compositor capture feed \u2014 a desktop window keyed by title, or a whole monitor keyed by index. The selector is the altitude of the primitive: MonitorIndex null is window mode; non-null is whole-monitor mode (and WindowTitle is unused).",
      "properties": {
        "$type": {
          "const": "capture"
        },
        "windowTitle": {
          "description": "The captured window\u0027s title (window mode; ignored when MonitorIndex is set).",
          "type": "string"
        },
        "profile": {
          "$ref": "#/$defs/WorldFeedProfileNonNullable",
          "description": "This capture consumer\u0027s output extent and maximum refresh cadence."
        },
        "monitorIndex": {
          "description": "The 0-based monitor to capture whole (0 = primary), or null for window mode.",
          "type": [
            "integer",
            "null"
          ],
          "default": null
        }
      },
      "required": [
        "windowTitle",
        "profile"
      ],
      "additionalProperties": false
    },
    "WorldScreenSourceConsole": {
      "description": "A screen showing the developer console as an object in the world \u2014 the diegetic half of the control plane the unification contract names (\u0022the on-screen panel and process stdin\u0022). The frame is CPU-composed into a CRT-styled framebuffer and pushed through IGpuSurfaceUpload, exactly as the ported console feed does; nothing about it is a render-graph node. Complementary to \u2014 never a duplicate of \u2014 ConsoleTape, which publishes the same content to the screen-space overlay. At most one console source may be live (declared) at a time; an unselected console entry sitting in a magazine is legal.",
      "properties": {
        "$type": {
          "const": "console"
        },
        "rows": {
          "description": "Console text rows the framebuffer composes, 1..120. Sizes the CPU buffer.",
          "type": "integer",
          "default": 24
        },
        "columns": {
          "description": "Console text columns, 1..400.",
          "type": "integer",
          "default": 64
        },
        "procedural": {
          "description": "When true the slot shows the sibling generated pattern instead of console text \u2014 carried as a mode of this variant rather than as a seventh union case.",
          "type": "boolean",
          "default": false
        }
      },
      "additionalProperties": false
    },
    "WorldScreenSourceQr": {
      "description": "An authorable QR code (ISO/IEC 18004) \u2014 the document names a payload string and the engine derives the scannable module grid (QrEncoder), rendered CPU-side into a static B8G8R8A8 framebuffer and uploaded once, never re-derived from the tick like TestPattern. The driving case is a link one human hands another off an in-world screen. This record is the document-authored half only \u2014 nothing here mints a payload at runtime; screen.source \u003Cindex\u003E qr is the live-authoring twin, and world.identify is the one caller that mints its payload (the running world\u0027s own documentId and content-address pin) rather than being handed one.",
      "properties": {
        "$type": {
          "const": "qr"
        },
        "payload": {
          "description": "The encoded string, UTF-8 byte mode. Must fit within version MaxSupportedVersion at EcLevel \u2014 validation refuses an oversized payload by name (its byte count against the level\u0027s capacity), never truncates it.",
          "type": "string"
        },
        "ecLevel": {
          "description": "The error-correction level: L, M, Q, or H (case-insensitive, parsed by TryParse). Defaults to M.",
          "type": "string",
          "default": "M"
        },
        "quietZoneModules": {
          "description": "The white quiet-zone border width in modules on every side. ISO/IEC 18004 recommends at least 4; a smaller value authors a QR a real scanner may refuse to read (a borderless QR does not scan) \u2014 the document may still author it (validation only refuses a negative width), since a screen\u0027s physical framing sometimes supplies the margin itself.",
          "type": "integer",
          "default": 4
        }
      },
      "required": [
        "payload"
      ],
      "additionalProperties": false
    },
    "WorldScreenSourceSession": {
      "description": "A live rendered view of another world, resolved through a destinations row (docs/vision.md, \u0022Observation and display\u0022). The face/screen resolves the same resolver-owned identity a portal crossing at the same door would land in (WorldSessionResolver), attaches an observation lease to the resolved instance\u0027s server, and mirrors just enough of its delivered definition/snapshots to render its static authored geometry through CameraName (or the destination\u0027s default projection). It never re-derives durability/scope/generation itself \u2014 those are the destination row\u0027s own facts.",
      "properties": {
        "$type": {
          "const": "session"
        },
        "destination": {
          "description": "The Name this face/screen observes. Must resolve to a declared destinations row \u2014 an undeclared name refuses at boot (validated, like a portal facet\u0027s own destination).",
          "type": "string"
        },
        "camera": {
          "description": "The destination\u0027s own placeable-camera name to render through, or null for its default projection (its first declared camera, else a fixed overview derived from its spawn points). Wire name camera \u2014 plain Camera would collide with the sibling Camera arm\u0027s own type name inside this enclosing record. Validated only as non-empty when present at author time \u2014 the destination\u0027s own definition is not joined at boot (references assert naming intent, not reachability), so an unknown camera name is refused loudly at bind time instead, once the destination is actually resolved, falling back to the default projection rather than refusing the whole bind. Ignored under Window (see Projection).",
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "projection": {
          "description": "How the destination render projects onto this face (see WorldScreenProjection). Default Camera \u2014 unauthored worlds and every session facet authored before this member existed render byte-identically. Optional and trailing (the same widen-without-moving-existing-members shape CameraName itself already follows). Window requires this same face\u0027s Portal to author Mapped with a Counterpart \u2014 refused by name otherwise (see WorldDefinitionValidator); a top-level screens row or magazine entry carries no face to pair with, so window is refused there unconditionally.",
          "enum": [
            "Camera",
            "Window"
          ],
          "default": "Camera"
        },
        "resolution": {
          "description": "The offscreen target\u0027s [width, height] in pixels, or null for the engine default (Puck.SdfVm.Views.WorldSessionView.DefaultWidth x DefaultHeight \u2014 today\u0027s 160x144 panel, unchanged for an unauthored facet). Each axis is validated within 1..WorldDefinitionValidator.MaxSurfaceDimension. Omitted from the wire when null.",
          "default": null
        }
      },
      "required": [
        "destination"
      ],
      "additionalProperties": false
    },
    "StringList": {
      "type": "array",
      "items": {
        "type": [
          "string",
          "null"
        ]
      }
    },
    "WorldScreenSourceText": {
      "description": "Authored reading text on the screen face, rendered through the engine\u0027s glyph-decal tier (Puck.SdfVm.SdfWorldEngine.SetScreenDecal): a fixed monospace cell grid sampled from the world\u0027s packed font atlas at shade time \u2014 the dense-text sibling of a creation\u0027s textRuns, which stamp marched Glyph geometry. Signs, plaques, books, and monitors author this; short sculptural lettering stays a text run. Requires the world to declare a text font catalog (Text); the decal bypasses the CRT image pipeline, so no image source competes with it on the slot.",
      "properties": {
        "$type": {
          "const": "text"
        },
        "lines": {
          "$ref": "#/$defs/StringList",
          "description": "The text rows, top-down; each row maps onto one grid row of cells (row-major, one scalar per cell \u2014 no kerning or shaping on this tier)."
        },
        "font": {
          "description": "The text catalog font name; null selects the catalog\u0027s default font.",
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "columns": {
          "description": "The cell grid\u0027s column count, or null to fit the widest line. The grid (columns x rows) is capped by the engine\u0027s per-screen decal cell budget (Puck.SignedDistance.SdfScreenDecalLayout.MaxScreenDecalCells).",
          "type": [
            "integer",
            "null"
          ],
          "default": null
        },
        "rows": {
          "description": "The cell grid\u0027s row count, or null to fit the line count.",
          "type": [
            "integer",
            "null"
          ],
          "default": null
        },
        "foreground": {
          "description": "The letter color as #RRGGBB, or null for white.",
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "background": {
          "description": "The cell background color as #RRGGBB, or null for black.",
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      },
      "required": [
        "lines"
      ],
      "additionalProperties": false
    },
    "WorldScreenSourceProbe": {
      "description": "A declared probe\u0027s texture output \u2014 the frame a kind that declares an output writes each cycle (a relit camera frame, a mask), published like a camera feed. The probe must be a probes row of this document; whether its kind writes a texture is checked at boot against the kind\u0027s manifest.",
      "properties": {
        "$type": {
          "const": "probe"
        },
        "id": {
          "description": "The probes[].id whose output this slot shows.",
          "type": "string"
        }
      },
      "required": [
        "id"
      ],
      "additionalProperties": false
    },
    "WorldScreenSource": {
      "description": "The signal carried by a WorldScreen\u0027s lit face. A source declares which provider feeds a slot; the engine resolves and samples it. The $type string is the JSON discriminator; a new source kind is a new derived record plus its JsonDerivedTypeAttribute line.",
      "type": "object",
      "required": [
        "$type"
      ],
      "anyOf": [
        {
          "$ref": "#/$defs/WorldScreenSourceNone"
        },
        {
          "$ref": "#/$defs/WorldScreenSourceTestPattern"
        },
        {
          "$ref": "#/$defs/WorldScreenSourceMachine"
        },
        {
          "$ref": "#/$defs/WorldScreenSourceCamera"
        },
        {
          "$ref": "#/$defs/WorldScreenSourceView"
        },
        {
          "$ref": "#/$defs/WorldScreenSourceCapture"
        },
        {
          "$ref": "#/$defs/WorldScreenSourceConsole"
        },
        {
          "$ref": "#/$defs/WorldScreenSourceQr"
        },
        {
          "$ref": "#/$defs/WorldScreenSourceSession"
        },
        {
          "$ref": "#/$defs/WorldScreenSourceText"
        },
        {
          "$ref": "#/$defs/WorldScreenSourceProbe"
        }
      ]
    },
    "WorldSolid": {
      "description": "A row\u0027s solidity facet \u2014 it participates in contact resolution using its own declared shape. Presence is the whole switch; null means decoration \u2014 the row is drawn but bodies pass through it.",
      "type": [
        "object",
        "null"
      ],
      "properties": {
        "margin": {
          "description": "The signed skin added to the shape for contact purposes. Positive fattens the collider past the drawn surface; negative lets a body sink in. Compensates the smooth-union blend.",
          "type": "number"
        }
      },
      "required": [
        "margin"
      ],
      "additionalProperties": false
    },
    "WorldAnchorEntity": {
      "description": "Rides one population entity\u0027s ROOT pose \u2014 a walking avatar\u0027s whole-body position and orientation.",
      "properties": {
        "$type": {
          "const": "entity"
        },
        "index": {
          "description": "The 0-based entity index, bounded by the world\u0027s authored population capacity.",
          "type": "integer"
        }
      },
      "required": [
        "index"
      ],
      "additionalProperties": false
    },
    "WorldAnchorEntityPart": {
      "description": "Rides one entity look\u0027s authored part pose rather than its whole-body root. The active look publishes the mapping from PartId to its packed transform slot; the slot remains an engine detail.",
      "properties": {
        "$type": {
          "const": "entityPart"
        },
        "index": {
          "description": "The 0-based entity index.",
          "type": "integer"
        },
        "partId": {
          "description": "The ordinal, case-sensitive part identifier published by the entity\u0027s active look.",
          "type": "string"
        }
      },
      "required": [
        "index",
        "partId"
      ],
      "additionalProperties": false
    },
    "WorldAnchorPlacement": {
      "description": "Rides a placement INSTANCE\u0027s stamped transform \u2014 a creation stamped into the world by reference (the same placement-reference shape CreationCameraDocument uses), optionally narrowed to one of its own authored shapes rather than the stamp\u0027s root.",
      "properties": {
        "$type": {
          "const": "placement"
        },
        "placementId": {
          "description": "The referenced Id (must resolve).",
          "type": "string"
        },
        "shapeId": {
          "description": "The referenced creation\u0027s ShapeDocument.Id to ride, or null for the placement\u0027s own stamped root transform.",
          "type": [
            "integer",
            "null"
          ]
        }
      },
      "required": [
        "placementId",
        "shapeId"
      ],
      "additionalProperties": false
    },
    "Int32List": {
      "type": [
        "array",
        "null"
      ],
      "items": {
        "type": "integer"
      }
    },
    "WorldAnchorGroup": {
      "description": "Rides the smoothed CENTROID of a set of population entities \u2014 the establishing-shot anchor. Also publishes the set\u0027s SPREAD (mean distance from the centroid), which a camera program\u0027s Offset consumes through its SpreadPullback. A group has no facing, so its orientation resolves to identity.",
      "properties": {
        "$type": {
          "const": "group"
        },
        "indices": {
          "$ref": "#/$defs/Int32List",
          "description": "The 0-based entity indices in the set, or null for the whole live population (every active entity). Each index is validated 0..127."
        },
        "smoothRate": {
          "description": "The exponential smoothing rate (per second) the centroid/spread ease at (validated positive and finite) \u2014 seeded un-smoothed on first resolve so a camera does not fly in from the origin.",
          "type": "number"
        }
      },
      "required": [
        "indices",
        "smoothRate"
      ],
      "additionalProperties": false
    },
    "WorldCameraSubjectReference": {
      "description": "The program\u0027s externally supplied reference pose \u2014 a named camera\u0027s own WorldAnchor, or a seat rig\u0027s currently perceived body (the seat\u0027s own avatar, or a possessed camera body). Resolved outside the program and handed to it as the SdfAnchor the compiled rig receives every frame.",
      "properties": {
        "$type": {
          "const": "reference"
        }
      },
      "additionalProperties": false
    },
    "WorldCameraSubjectPlacement": {
      "description": "Rides a placement\u0027s authored stamped transform (position only \u2014 a placement carries no live facing through this static resolve, the same limitation a placeable camera\u0027s own Placement arm and a speaker share).",
      "properties": {
        "$type": {
          "const": "placement"
        },
        "placementId": {
          "description": "The referenced Id (must resolve).",
          "type": "string"
        },
        "shapeId": {
          "description": "The referenced creation\u0027s own shape to ride, or null for the placement\u0027s stamped root transform.",
          "type": [
            "integer",
            "null"
          ],
          "default": null
        }
      },
      "required": [
        "placementId"
      ],
      "additionalProperties": false
    },
    "WorldCameraSubjectWorldPoint": {
      "description": "A fixed world-space point.",
      "properties": {
        "$type": {
          "const": "worldPoint"
        },
        "point": {
          "description": "The world-space position."
        }
      },
      "required": [
        "point"
      ],
      "additionalProperties": false
    },
    "WorldCameraSubject": {
      "description": "The subject an Anchor/LookAt op resolves against \u2014 the closed \u0022what a camera program can key off\u0022 vocabulary. Distinct from WorldAnchor (WHERE a whole placeable camera or speaker rides, resolved OUTSIDE the program and handed in as one reference pose): this is presentation math INSIDE the program, so it stays float and needs no live entity table \u2014 a placement\u0027s pose resolves through the same static stamped-transform math a placeable camera\u0027s own anchor and a speaker read (Puck.World.Client.WorldAnchorGeometry).",
      "type": "object",
      "required": [
        "$type"
      ],
      "anyOf": [
        {
          "$ref": "#/$defs/WorldCameraSubjectReference"
        },
        {
          "$ref": "#/$defs/WorldCameraSubjectPlacement"
        },
        {
          "$ref": "#/$defs/WorldCameraSubjectWorldPoint"
        }
      ]
    },
    "WorldCameraProgramOpAnchor": {
      "description": "Establishes the CURRENT subject \u2014 the pose Offset/Orbit place the eye relative to, and LookAt aims along the facing of when it names no subject of its own. Also re-seeds the eye at the resolved subject\u0027s own position (the \u0022first person\u0022 default before any Offset/Orbit runs). At most one per program, and it must be the first operation when present; a program that omits it starts from Reference implicitly.",
      "properties": {
        "$type": {
          "const": "anchor"
        },
        "subject": {
          "$ref": "#/$defs/WorldCameraSubject",
          "description": "The subject to resolve."
        },
        "opcode": {
          "description": "Gets this op\u0027s authored $type token \u2014 the one spelling a refusal, a read-back, and the document all use.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "subject"
      ],
      "additionalProperties": false
    },
    "WorldCameraProgramOpOffset": {
      "description": "Places the eye at an offset from the current subject\u0027s pose.",
      "properties": {
        "$type": {
          "const": "offset"
        },
        "value": {
          "description": "The offset."
        },
        "worldAxes": {
          "description": "Whether Value uses world axes rather than the subject\u0027s own.",
          "type": "boolean",
          "default": false
        },
        "spreadPullback": {
          "description": "The group-spread multiplier applied to the offset \u2014 meaningful only when the program\u0027s OWN externally supplied reference is a Group establishing shot (zero, and thus inert, everywhere else).",
          "type": "number",
          "default": 0
        },
        "opcode": {
          "description": "Gets this op\u0027s authored $type token \u2014 the one spelling a refusal, a read-back, and the document all use.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "value"
      ],
      "additionalProperties": false
    },
    "WorldCameraSubjectNullable": {
      "description": "The subject an Anchor/LookAt op resolves against \u2014 the closed \u0022what a camera program can key off\u0022 vocabulary. Distinct from WorldAnchor (WHERE a whole placeable camera or speaker rides, resolved OUTSIDE the program and handed in as one reference pose): this is presentation math INSIDE the program, so it stays float and needs no live entity table \u2014 a placement\u0027s pose resolves through the same static stamped-transform math a placeable camera\u0027s own anchor and a speaker read (Puck.World.Client.WorldAnchorGeometry).",
      "type": [
        "object",
        "null"
      ],
      "required": [
        "$type"
      ],
      "anyOf": [
        {
          "$ref": "#/$defs/WorldCameraSubjectReference"
        },
        {
          "$ref": "#/$defs/WorldCameraSubjectPlacement"
        },
        {
          "$ref": "#/$defs/WorldCameraSubjectWorldPoint"
        }
      ]
    },
    "WorldCameraProgramOpLookAt": {
      "description": "Sets the aim target.",
      "properties": {
        "$type": {
          "const": "lookAt"
        },
        "subject": {
          "$ref": "#/$defs/WorldCameraSubjectNullable",
          "description": "The subject to look at, or null to look along the current subject\u0027s own forward axis from the eye, at FocusDistance."
        },
        "targetOffset": {
          "description": "An offset from the resolved Subject\u0027s pose; ignored when Subject is null.",
          "default": null
        },
        "worldAxes": {
          "description": "Whether TargetOffset uses world axes rather than the subject\u0027s own.",
          "type": "boolean",
          "default": false
        },
        "focusDistance": {
          "description": "The finite target distance along the current subject\u0027s forward axis, used only when Subject is null.",
          "type": "number",
          "default": 6
        },
        "opcode": {
          "description": "Gets this op\u0027s authored $type token \u2014 the one spelling a refusal, a read-back, and the document all use.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "subject"
      ],
      "additionalProperties": false
    },
    "WorldCameraProgramOpOrbit": {
      "description": "Orbits the eye about its pivot. Yaw and Pitch are live-bindable like Fov\u0027s field of view: a seat rig whose yaw reads a state cell turns the CAMERA when that cell changes \u2014 look behind is state.look.behind flipping between 0 and \u03C0 \u2014 while the seat\u0027s facing (what steering and movement resolve against) is untouched, because the facing never includes the rig\u0027s offsets.",
      "properties": {
        "$type": {
          "const": "orbit"
        },
        "distance": {
          "type": "number"
        },
        "yaw": {},
        "pitch": {},
        "pivotOffset": {
          "default": null
        },
        "opcode": {
          "description": "Gets this op\u0027s authored $type token \u2014 the one spelling a refusal, a read-back, and the document all use.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "distance",
        "yaw",
        "pitch"
      ],
      "additionalProperties": false
    },
    "WorldCameraProgramOpSmooth": {
      "description": "Sets the presentation-only exponential response rate (per second) the resolved eye/target boom eases at; zero disables smoothing. Read by the caller after resolving a frame \u2014 never affects the resolved pose itself. At most one per program.",
      "properties": {
        "$type": {
          "const": "smooth"
        },
        "rate": {
          "description": "The non-negative response rate.",
          "type": "number"
        },
        "opcode": {
          "description": "Gets this op\u0027s authored $type token \u2014 the one spelling a refusal, a read-back, and the document all use.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "rate"
      ],
      "additionalProperties": false
    },
    "WorldCameraProgramOpClampPitch": {
      "description": "Clamps the effective pitch the NEXT Orbit op resolves with (its authored Pitch plus any live seat delta) to [MinPitch, MaxPitch]. At most one per program; must precede the Orbit op it governs. A joined seat\u0027s own views.seatControl band already clamps its LIVE delta before this ever runs \u2014 this op exists for a program with no seat behind it (a named camera orbiting a state-bound pitch).",
      "properties": {
        "$type": {
          "const": "clampPitch"
        },
        "minPitch": {
          "description": "The minimum pitch, radians.",
          "type": "number"
        },
        "maxPitch": {
          "description": "The maximum pitch, radians.",
          "type": "number"
        },
        "opcode": {
          "description": "Gets this op\u0027s authored $type token \u2014 the one spelling a refusal, a read-back, and the document all use.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "minPitch",
        "maxPitch"
      ],
      "additionalProperties": false
    },
    "WorldCameraProgramOpFov": {
      "description": "Sets the rendered vertical field of view, radians \u2014 a literal, or a state.\u003Crow\u003E[.\u003Ckey\u003E] binding so a world rule can pull focus or frame a moment (decisions in the sim, framing in presentation). At most one per program.",
      "properties": {
        "$type": {
          "const": "fov"
        },
        "fieldOfViewRadians": {
          "description": "The field of view."
        },
        "opcode": {
          "description": "Gets this op\u0027s authored $type token \u2014 the one spelling a refusal, a read-back, and the document all use.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "fieldOfViewRadians"
      ],
      "additionalProperties": false
    },
    "WorldCameraProgramOpBlend": {
      "description": "Evaluates two other authored programs by name and linearly interpolates their resolved eye, target, and field of view \u2014 the whole document\u0027s camera-program table (every cameras[].rig plus views.seatRig/views.cameraRig) is the namespace A/B resolve against. At most one per program; refused when it would create a reference cycle.",
      "properties": {
        "$type": {
          "const": "blend"
        },
        "a": {
          "description": "The program resolved at Weight 0.",
          "type": "string"
        },
        "b": {
          "description": "The program resolved at Weight 1.",
          "type": "string"
        },
        "weight": {
          "description": "The blend weight in [0, 1] \u2014 a literal, or a state binding."
        },
        "opcode": {
          "description": "Gets this op\u0027s authored $type token \u2014 the one spelling a refusal, a read-back, and the document all use.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "a",
        "b",
        "weight"
      ],
      "additionalProperties": false
    },
    "WorldCameraProgramOp": {
      "description": "One instruction in an authored camera program\u0027s ordered op list \u2014 the presentation-side pose algebra bodyMotionPrograms established for sim-side movement, promoted to cameras: an authored rig is an ordered list of these trivial ops rather than a bespoke closed motion/aim union, so a new camera behavior is authored data, never new engine code. This is the AUTHORING vocabulary only: Puck.World.Client.WorldCameraRigCompiler translates it to Puck.SdfVm.Views.SdfCameraOp and resolves each frame\u0027s bindings and subject poses, and Puck.SdfVm.Views.SdfCameraProgramEvaluator \u2014 which parses no document \u2014 walks the result. Floats throughout; presentation carries no fixed-point burden.",
      "type": [
        "object",
        "null"
      ],
      "required": [
        "$type"
      ],
      "anyOf": [
        {
          "$ref": "#/$defs/WorldCameraProgramOpAnchor"
        },
        {
          "$ref": "#/$defs/WorldCameraProgramOpOffset"
        },
        {
          "$ref": "#/$defs/WorldCameraProgramOpLookAt"
        },
        {
          "$ref": "#/$defs/WorldCameraProgramOpOrbit"
        },
        {
          "$ref": "#/$defs/WorldCameraProgramOpSmooth"
        },
        {
          "$ref": "#/$defs/WorldCameraProgramOpClampPitch"
        },
        {
          "$ref": "#/$defs/WorldCameraProgramOpFov"
        },
        {
          "$ref": "#/$defs/WorldCameraProgramOpBlend"
        }
      ]
    },
    "WorldCameraProgramOpList": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/WorldCameraProgramOp"
      }
    },
    "WorldCameraProgramOpAnchorNullable": {
      "description": "Establishes the CURRENT subject \u2014 the pose Offset/Orbit place the eye relative to, and LookAt aims along the facing of when it names no subject of its own. Also re-seeds the eye at the resolved subject\u0027s own position (the \u0022first person\u0022 default before any Offset/Orbit runs). At most one per program, and it must be the first operation when present; a program that omits it starts from Reference implicitly.",
      "type": [
        "object",
        "null"
      ],
      "properties": {
        "subject": {
          "$ref": "#/$defs/WorldCameraSubject",
          "description": "The subject to resolve."
        },
        "opcode": {
          "description": "Gets this op\u0027s authored $type token \u2014 the one spelling a refusal, a read-back, and the document all use.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "subject"
      ],
      "additionalProperties": false
    },
    "WorldCameraProgramOpBlendNullable": {
      "description": "Evaluates two other authored programs by name and linearly interpolates their resolved eye, target, and field of view \u2014 the whole document\u0027s camera-program table (every cameras[].rig plus views.seatRig/views.cameraRig) is the namespace A/B resolve against. At most one per program; refused when it would create a reference cycle.",
      "type": [
        "object",
        "null"
      ],
      "properties": {
        "a": {
          "description": "The program resolved at Weight 0.",
          "type": "string"
        },
        "b": {
          "description": "The program resolved at Weight 1.",
          "type": "string"
        },
        "weight": {
          "description": "The blend weight in [0, 1] \u2014 a literal, or a state binding."
        },
        "opcode": {
          "description": "Gets this op\u0027s authored $type token \u2014 the one spelling a refusal, a read-back, and the document all use.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "a",
        "b",
        "weight"
      ],
      "additionalProperties": false
    },
    "WorldCameraProgramOpClampPitchNullable": {
      "description": "Clamps the effective pitch the NEXT Orbit op resolves with (its authored Pitch plus any live seat delta) to [MinPitch, MaxPitch]. At most one per program; must precede the Orbit op it governs. A joined seat\u0027s own views.seatControl band already clamps its LIVE delta before this ever runs \u2014 this op exists for a program with no seat behind it (a named camera orbiting a state-bound pitch).",
      "type": [
        "object",
        "null"
      ],
      "properties": {
        "minPitch": {
          "description": "The minimum pitch, radians.",
          "type": "number"
        },
        "maxPitch": {
          "description": "The maximum pitch, radians.",
          "type": "number"
        },
        "opcode": {
          "description": "Gets this op\u0027s authored $type token \u2014 the one spelling a refusal, a read-back, and the document all use.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "minPitch",
        "maxPitch"
      ],
      "additionalProperties": false
    },
    "WorldCameraProgramOpFovNullable": {
      "description": "Sets the rendered vertical field of view, radians \u2014 a literal, or a state.\u003Crow\u003E[.\u003Ckey\u003E] binding so a world rule can pull focus or frame a moment (decisions in the sim, framing in presentation). At most one per program.",
      "type": [
        "object",
        "null"
      ],
      "properties": {
        "fieldOfViewRadians": {
          "description": "The field of view."
        },
        "opcode": {
          "description": "Gets this op\u0027s authored $type token \u2014 the one spelling a refusal, a read-back, and the document all use.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "fieldOfViewRadians"
      ],
      "additionalProperties": false
    },
    "WorldCameraProgramOpLookAtNullable": {
      "description": "Sets the aim target.",
      "type": [
        "object",
        "null"
      ],
      "properties": {
        "subject": {
          "$ref": "#/$defs/WorldCameraSubjectNullable",
          "description": "The subject to look at, or null to look along the current subject\u0027s own forward axis from the eye, at FocusDistance."
        },
        "targetOffset": {
          "description": "An offset from the resolved Subject\u0027s pose; ignored when Subject is null.",
          "default": null
        },
        "worldAxes": {
          "description": "Whether TargetOffset uses world axes rather than the subject\u0027s own.",
          "type": "boolean",
          "default": false
        },
        "focusDistance": {
          "description": "The finite target distance along the current subject\u0027s forward axis, used only when Subject is null.",
          "type": "number",
          "default": 6
        },
        "opcode": {
          "description": "Gets this op\u0027s authored $type token \u2014 the one spelling a refusal, a read-back, and the document all use.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "subject"
      ],
      "additionalProperties": false
    },
    "WorldCameraProgramOpOffsetNullable": {
      "description": "Places the eye at an offset from the current subject\u0027s pose.",
      "type": [
        "object",
        "null"
      ],
      "properties": {
        "value": {
          "description": "The offset."
        },
        "worldAxes": {
          "description": "Whether Value uses world axes rather than the subject\u0027s own.",
          "type": "boolean",
          "default": false
        },
        "spreadPullback": {
          "description": "The group-spread multiplier applied to the offset \u2014 meaningful only when the program\u0027s OWN externally supplied reference is a Group establishing shot (zero, and thus inert, everywhere else).",
          "type": "number",
          "default": 0
        },
        "opcode": {
          "description": "Gets this op\u0027s authored $type token \u2014 the one spelling a refusal, a read-back, and the document all use.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "value"
      ],
      "additionalProperties": false
    },
    "WorldCameraProgramOpOrbitNullable": {
      "description": "Orbits the eye about its pivot. Yaw and Pitch are live-bindable like Fov\u0027s field of view: a seat rig whose yaw reads a state cell turns the CAMERA when that cell changes \u2014 look behind is state.look.behind flipping between 0 and \u03C0 \u2014 while the seat\u0027s facing (what steering and movement resolve against) is untouched, because the facing never includes the rig\u0027s offsets.",
      "type": [
        "object",
        "null"
      ],
      "properties": {
        "distance": {
          "type": "number"
        },
        "yaw": {},
        "pitch": {},
        "pivotOffset": {
          "default": null
        },
        "opcode": {
          "description": "Gets this op\u0027s authored $type token \u2014 the one spelling a refusal, a read-back, and the document all use.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "distance",
        "yaw",
        "pitch"
      ],
      "additionalProperties": false
    },
    "WorldCameraProgramOpSmoothNullable": {
      "description": "Sets the presentation-only exponential response rate (per second) the resolved eye/target boom eases at; zero disables smoothing. Read by the caller after resolving a frame \u2014 never affects the resolved pose itself. At most one per program.",
      "type": [
        "object",
        "null"
      ],
      "properties": {
        "rate": {
          "description": "The non-negative response rate.",
          "type": "number"
        },
        "opcode": {
          "description": "Gets this op\u0027s authored $type token \u2014 the one spelling a refusal, a read-back, and the document all use.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "rate"
      ],
      "additionalProperties": false
    },
    "WorldCameraProgram": {
      "description": "An authored camera program \u2014 the ordered op list a rig resolves through every frame.",
      "type": "object",
      "properties": {
        "name": {
          "description": "The stable name a Blend op selects this program by.",
          "type": "string"
        },
        "version": {
          "description": "The instruction-set version.",
          "type": "string"
        },
        "operations": {
          "$ref": "#/$defs/WorldCameraProgramOpList",
          "description": "The selected ops, in authored evaluation order."
        },
        "anchorOp": {
          "$ref": "#/$defs/WorldCameraProgramOpAnchorNullable",
          "description": "Gets the program\u0027s Anchor op, or null when absent (an absent anchor starts from Reference implicitly)."
        },
        "blendOp": {
          "$ref": "#/$defs/WorldCameraProgramOpBlendNullable",
          "description": "Gets the program\u0027s Blend op, or null."
        },
        "clampPitchOp": {
          "$ref": "#/$defs/WorldCameraProgramOpClampPitchNullable",
          "description": "Gets the program\u0027s ClampPitch op, or null."
        },
        "fovOp": {
          "$ref": "#/$defs/WorldCameraProgramOpFovNullable",
          "description": "Gets the program\u0027s Fov op, or null."
        },
        "lookAtOp": {
          "$ref": "#/$defs/WorldCameraProgramOpLookAtNullable",
          "description": "Gets the program\u0027s LookAt op, or null."
        },
        "offsetOp": {
          "$ref": "#/$defs/WorldCameraProgramOpOffsetNullable",
          "description": "Gets the program\u0027s Offset op, or null."
        },
        "orbitOp": {
          "$ref": "#/$defs/WorldCameraProgramOpOrbitNullable",
          "description": "Gets the program\u0027s Orbit op, or null."
        },
        "smoothOp": {
          "$ref": "#/$defs/WorldCameraProgramOpSmoothNullable",
          "description": "Gets the program\u0027s Smooth op, or null (rate 0 \u2014 no smoothing \u2014 when absent)."
        }
      },
      "required": [
        "name",
        "version",
        "operations"
      ],
      "additionalProperties": false
    },
    "WorldDistributionRegionDisc": {
      "description": "A planar disc centered on the consumer\u0027s origin.",
      "properties": {
        "$type": {
          "const": "disc"
        },
        "radius": {
          "description": "The disc radius.",
          "type": "number"
        },
        "sampleCount": {
          "description": "The radial fill count, or null to use the consumer\u0027s requested count.",
          "type": [
            "integer",
            "null"
          ],
          "default": null
        }
      },
      "required": [
        "radius"
      ],
      "additionalProperties": false
    },
    "WorldDistributionRegionPoints": {
      "description": "A cycle of authored spawn poses, each expanded by a planar square.",
      "properties": {
        "$type": {
          "const": "points"
        },
        "names": {
          "description": "The spawn-point names.",
          "type": "array",
          "items": {
            "type": [
              "string",
              "null"
            ]
          }
        },
        "halfExtent": {
          "description": "The square half-extent on X and Z.",
          "type": "number"
        }
      },
      "required": [
        "names",
        "halfExtent"
      ],
      "additionalProperties": false
    },
    "WorldDistributionRegionLattice": {
      "description": "A finite two-axis lattice local to a placement.",
      "properties": {
        "$type": {
          "const": "lattice"
        },
        "stepA": {
          "description": "The first per-copy step."
        },
        "countA": {
          "description": "The copy count along StepA.",
          "type": "integer"
        },
        "stepB": {
          "description": "The second per-copy step."
        },
        "countB": {
          "description": "The copy count along StepB.",
          "type": "integer"
        }
      },
      "required": [
        "stepA",
        "countA",
        "stepB",
        "countB"
      ],
      "additionalProperties": false
    },
    "WorldDistributionRegion": {
      "description": "A closed spatial region vocabulary consumed by WorldDistribution.",
      "type": "object",
      "required": [
        "$type"
      ],
      "anyOf": [
        {
          "$ref": "#/$defs/WorldDistributionRegionDisc"
        },
        {
          "$ref": "#/$defs/WorldDistributionRegionPoints"
        },
        {
          "$ref": "#/$defs/WorldDistributionRegionLattice"
        }
      ]
    },
    "WorldSequence": {
      "description": "A deterministic index-to-sample declaration shared by distributions, row assignment, color, and population variation. The document selects the sequence and its phase; the engine owns its exact arithmetic.",
      "type": "object",
      "properties": {
        "name": {
          "description": "The sequence name: None, Index, Additive, R1, or R2.",
          "type": "string"
        },
        "offset": {
          "description": "The signed phase added to the caller\u0027s stable index before sampling.",
          "type": "integer"
        },
        "step": {
          "description": "The turn-sized increment for Additive; zero for every other sequence.",
          "type": "number"
        }
      },
      "required": [
        "name",
        "offset",
        "step"
      ],
      "additionalProperties": false
    },
    "WorldDistribution": {
      "description": "A spatial region composed with the deterministic sequence that fills it.",
      "type": [
        "object",
        "null"
      ],
      "properties": {
        "region": {
          "$ref": "#/$defs/WorldDistributionRegion",
          "description": "The region to fill."
        },
        "fill": {
          "$ref": "#/$defs/WorldSequence",
          "description": "The per-index fill sequence."
        }
      },
      "required": [
        "region",
        "fill"
      ],
      "additionalProperties": false
    },
    "WorldPopulationVariation": {
      "description": "The three independently authored sequences that seed a body\u0027s producer state.",
      "type": [
        "object",
        "null"
      ],
      "properties": {
        "phase": {
          "$ref": "#/$defs/WorldSequence",
          "description": "The angular phase sequence."
        },
        "weave": {
          "$ref": "#/$defs/WorldSequence",
          "description": "The scalar weave-frequency variation sequence."
        },
        "activity": {
          "$ref": "#/$defs/WorldSequence",
          "description": "The paired activity-rate and altitude variation sequence."
        }
      },
      "required": [
        "phase",
        "weave",
        "activity"
      ],
      "additionalProperties": false
    },
    "WorldSequenceNullable": {
      "description": "A deterministic index-to-sample declaration shared by distributions, row assignment, color, and population variation. The document selects the sequence and its phase; the engine owns its exact arithmetic.",
      "type": [
        "object",
        "null"
      ],
      "properties": {
        "name": {
          "description": "The sequence name: None, Index, Additive, R1, or R2.",
          "type": "string"
        },
        "offset": {
          "description": "The signed phase added to the caller\u0027s stable index before sampling.",
          "type": "integer"
        },
        "step": {
          "description": "The turn-sized increment for Additive; zero for every other sequence.",
          "type": "number"
        }
      },
      "required": [
        "name",
        "offset",
        "step"
      ],
      "additionalProperties": false
    },
    "WorldGeneratorSource": {
      "description": "The closed vocabulary of a WorldGenerator\u0027s draw shape \u2014 which of its fields are read, and what one emission produces: a Markov text walk, a deck deal, a uniform range, a weighted numeric table, and a raw stream draw are sources of one family, never parallel primitives with their own seeding, cursoring, and refusal stories.",
      "enum": [
        "Markov",
        "UniformRange",
        "WeightedNumeric",
        "StreamDraw"
      ]
    },
    "WorldGeneratorAlternative": {
      "description": "One weighted alternative of a WorldGeneratorContext: the token it emits, its relative weight, and the context the walk moves into after it is picked. The authored Next is what makes this a real Markov process rather than a bag of independent draws \u2014 the context key is the process state, so an author folds exactly as much history into it as the chain needs.",
      "type": [
        "object",
        "null"
      ],
      "properties": {
        "token": {
          "description": "The opaque game-authored token this alternative emits. The engine never interprets it; it is space-joined with the emission\u0027s other tokens and written into the target text cell. Bounded by MaxTokenLength.",
          "type": "string"
        },
        "weight": {
          "description": "The alternative\u0027s positive relative weight. At least one alternative in a context must carry a non-zero weight.",
          "type": "integer"
        },
        "next": {
          "description": "The context the walk moves into after this alternative is picked. Must name a declared Key; naming a context that declares NO alternatives ends the emission (a terminal is a context with nothing to say, never a reserved token spelling).",
          "type": "string"
        }
      },
      "required": [
        "token",
        "weight",
        "next"
      ],
      "additionalProperties": false
    },
    "WorldGeneratorContext": {
      "description": "One named context of a WorldGenerator \u2014 the state the walk may be sitting in and the weighted alternatives it may pick while there. A context declaring NO alternatives is TERMINAL: reaching it ends the emission.",
      "type": [
        "object",
        "null"
      ],
      "properties": {
        "key": {
          "description": "The stable context key, unique within the generator.",
          "type": "string"
        },
        "alternatives": {
          "description": "The weighted alternatives out of this context, or empty for a terminal context.",
          "type": [
            "array",
            "null"
          ],
          "items": {
            "$ref": "#/$defs/WorldGeneratorAlternative"
          },
          "default": null
        }
      },
      "required": [
        "key"
      ],
      "additionalProperties": false
    },
    "WorldGeneratorContextList": {
      "type": [
        "array",
        "null"
      ],
      "items": {
        "$ref": "#/$defs/WorldGeneratorContext"
      }
    },
    "WorldGeneratorMode": {
      "description": "How a WorldGenerator context\u0027s alternatives are consumed \u2014 the deck vocabulary. Authored, never inferred: exhaustion behaviour is a declaration, not a fallback the engine picks.",
      "enum": [
        "WithReplacement",
        "WithoutReplacement",
        "ReshuffleOnExhaustion"
      ]
    },
    "WorldGeneratorWeightedNumeric": {
      "description": "One numeric outcome of a WeightedNumeric source: the raw value it writes and its relative weight \u2014 the numeric twin of WorldGeneratorAlternative, minus Token (nothing to join into text) and Next (a numeric draw is one terminal pick, never a walk).",
      "type": [
        "object",
        "null"
      ],
      "properties": {
        "value": {
          "description": "The raw value this outcome writes on selection (a plain integer for Int, raw FixedQ4816 bits for Fixed).",
          "type": "integer"
        },
        "weight": {
          "description": "The outcome\u0027s relative weight, fed straight to Puck.Maths.WeightedSampler\u0027s exact ulong overload. At least one outcome must carry a non-zero weight.",
          "type": "integer"
        }
      },
      "required": [
        "value",
        "weight"
      ],
      "additionalProperties": false
    },
    "WorldGeneratorWeightedNumericList": {
      "type": [
        "array",
        "null"
      ],
      "items": {
        "$ref": "#/$defs/WorldGeneratorWeightedNumeric"
      }
    },
    "WorldGenerator": {
      "description": "An authored stochastic source \u2014 the vocabulary for every randomness declaration in the document: a name generator, a dialogue line, a loot roll, a flat weighted draw, a card deal, a random census, and a drawn host backend all reduce to a source of this family, sampled at an authored moment into an authored site (see WorldDraw).",
      "type": [
        "object",
        "null"
      ],
      "properties": {
        "source": {
          "$ref": "#/$defs/WorldGeneratorSource",
          "description": "Which draw shape this source fires.",
          "default": "Markov"
        },
        "start": {
          "description": "Markov only: the context every emission begins from. Must name a declared context.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "bound": {
          "description": "Markov only: the maximum tokens one emission may draw before refusing by name, 1..MaxEmissionBound. Left at DefaultBound by a numeric source.",
          "type": "integer",
          "default": 1
        },
        "contexts": {
          "$ref": "#/$defs/WorldGeneratorContextList",
          "description": "Markov only: the declared contexts, at least one, uniquely keyed.",
          "default": null
        },
        "mode": {
          "$ref": "#/$defs/WorldGeneratorMode",
          "description": "Markov only: how alternatives are consumed (see WorldGeneratorMode).",
          "default": "WithReplacement"
        },
        "rangeMin": {
          "description": "UniformRange only: the closed range\u0027s inclusive lower bound \u2014 both bounds present or neither. Raw-encoded per the destination site\u0027s CellKind (raw FixedQ4816 bits for a fixed site) \u2014 unlike a site row\u0027s own min/max, which a fixed row authors as decimal text, since a source is not bound to one site and cannot know the kind it will write.",
          "type": [
            "integer",
            "null"
          ],
          "default": null
        },
        "rangeMax": {
          "description": "UniformRange only: the inclusive upper bound, same encoding as RangeMin.",
          "type": [
            "integer",
            "null"
          ],
          "default": null
        },
        "weighted": {
          "$ref": "#/$defs/WorldGeneratorWeightedNumericList",
          "description": "WeightedNumeric only: the weighted numeric outcomes, at least one, at least one carrying a non-zero weight.",
          "default": null
        }
      },
      "additionalProperties": false
    },
    "WorldDraw": {
      "description": "The AUTHORED-RANDOMNESS facet: the declaration that a SITE\u0027s value is DRAWN rather than literal. One facet, one source family, one engine \u2014 an NPC-bark text site, a loot cell, a random census, and a drawn host backend are the SAME mechanism pointed at different sites.",
      "type": [
        "object",
        "null"
      ],
      "properties": {
        "source": {
          "description": "The declared source to draw from, by name \u2014 or null when Generator inlines one.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "generator": {
          "$ref": "#/$defs/WorldGenerator",
          "description": "An inline anonymous source \u2014 or null when Source names a declared one.",
          "default": null
        },
        "timing": {
          "description": "When this site draws and whether it may be redrawn (see WorldDrawTiming).",
          "enum": [
            "Boot",
            "TickPeriod",
            "Event"
          ],
          "default": "Boot"
        }
      },
      "additionalProperties": false
    },
    "ActionFact": {
      "enum": [
        "Grounded",
        "Airborne",
        "Rising",
        "Falling",
        "AffectedBy",
        "Submerged",
        "AtSurface"
      ]
    },
    "ActionPredicateNow": {
      "description": "The fact holds this tick.",
      "properties": {
        "$type": {
          "const": "now"
        },
        "fact": {
          "$ref": "#/$defs/ActionFact"
        }
      },
      "required": [
        "fact"
      ],
      "additionalProperties": false
    },
    "ActionPredicateRecently": {
      "description": "The fact held within the last WindowSeconds \u2014 a per-instance recency clock, refreshed while the fact holds and decaying otherwise (coyote time is Recently(Grounded, w)).",
      "properties": {
        "$type": {
          "const": "recently"
        },
        "fact": {
          "$ref": "#/$defs/ActionFact"
        },
        "windowSeconds": {
          "type": "number"
        }
      },
      "required": [
        "fact",
        "windowSeconds"
      ],
      "additionalProperties": false
    },
    "ActionStateComparison": {
      "enum": [
        "Equal",
        "NotEqual",
        "Less",
        "LessOrEqual",
        "Greater",
        "GreaterOrEqual"
      ]
    },
    "ActionPredicateCompareState": {
      "description": "Compares a named state cell against either a fixed authored value, or \u2014 world scope only \u2014 another named state cell/reserved channel read live at the same evaluation. Both spellings are authorable; exactly one of Value and ComparandState may be present (refused by name when both or neither are). The comparand-row spelling is what lets a gate track a moving threshold \u2014 $tick compared against a schedule row the rule\u0027s own effects advance is \u0022every N ticks\u0022; a round row compared against a declared length row is a round boundary \u2014 composition over the same two-sided comparison, never a new mechanism.",
      "properties": {
        "$type": {
          "const": "compareState"
        },
        "state": {
          "description": "At body scope, a named counter slot the kit declares. At world scope (see WorldRule), a declared state-section row name, or one of WorldRuleFacts\u0027s reserved channels.",
          "type": "string"
        },
        "comparison": {
          "$ref": "#/$defs/ActionStateComparison",
          "description": "The comparison to apply."
        },
        "value": {
          "description": "The authored constant comparand, or null when ComparandState spells the comparand instead. Required (non-null) at body scope, where a comparand row reference is refused.",
          "type": [
            "number",
            "null"
          ],
          "default": null
        },
        "key": {
          "description": "At world scope, the cell inside State to read \u2014 null reads the row\u0027s slot cell, which a keyed row does not have (refused by name rather than silently reading cells[0]). At body scope a non-null key is refused: a per-body action-state slot is not keyed, and a parsed-and-discarded field is worse than no field.",
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "comparandState": {
          "description": "world scope only (refused at body scope, on the same terms as Key): another declared state-section row name, or one of WorldRuleFacts\u0027s reserved channels, read live and compared instead of Value. A dotted spelling (an author reaching for row.key in one string) is refused by name \u2014 address the cell with ComparandKey instead. Comparing across incompatible cell kinds (an int row against a fixed row, say) is refused by name \u2014 mixing scales silently is worse than naming the mismatch.",
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "comparandKey": {
          "description": "The cell inside ComparandState, on the same (row, key) terms as Key. Refused when ComparandState names a reserved channel or is absent.",
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      },
      "required": [
        "state",
        "comparison"
      ],
      "additionalProperties": false
    },
    "ActionPredicateTimerElapsed": {
      "description": "Whether a named timer slot has drained.",
      "properties": {
        "$type": {
          "const": "timerElapsed"
        },
        "state": {
          "type": "string"
        }
      },
      "required": [
        "state"
      ],
      "additionalProperties": false
    },
    "ActionPredicateAll": {
      "description": "Every inner predicate holds (conjunction).",
      "properties": {
        "$type": {
          "const": "all"
        },
        "predicates": {
          "$ref": "#/$defs/ActionPredicateList"
        }
      },
      "required": [
        "predicates"
      ],
      "additionalProperties": false
    },
    "ActionPredicate": {
      "description": "A data-composable gate over body facts and named action state. A trigger fires only while its gate holds. The $type string is the JSON discriminator, the same convention every polymorphic row family uses; a new predicate kind is a new derived record plus its JsonDerivedTypeAttribute line.",
      "type": [
        "object",
        "null"
      ],
      "required": [
        "$type"
      ],
      "anyOf": [
        {
          "$ref": "#/$defs/ActionPredicateNow"
        },
        {
          "$ref": "#/$defs/ActionPredicateRecently"
        },
        {
          "$ref": "#/$defs/ActionPredicateCompareState"
        },
        {
          "$ref": "#/$defs/ActionPredicateTimerElapsed"
        },
        {
          "$ref": "#/$defs/ActionPredicateAll"
        }
      ]
    },
    "ActionPredicateList": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/ActionPredicate"
      }
    },
    "MotionResponse": {
      "description": "One row of a kit\u0027s velocity-response table: how fast planar velocity converges on the commanded target while Gate holds. Rows evaluate in order, first match wins; a body matching no row snaps instantly (the built-in behavior, and the behavior of a kit with no table). The gate reuses the action-lane predicate vocabulary \u2014 only body-fact kinds (now/recently/all) are admissible.",
      "type": [
        "object",
        "null"
      ],
      "properties": {
        "engageRate": {
          "description": "The convergence rate (world units/second\u00B2) while the stick is deflected \u2014 acceleration toward the commanded target.",
          "type": "number"
        },
        "releaseRate": {
          "description": "The convergence rate while the stick is centered \u2014 deceleration toward rest (the coast).",
          "type": "number"
        },
        "gate": {
          "$ref": "#/$defs/ActionPredicate",
          "description": "The body-fact predicate that must hold for this row to win, or null for the always-row (permitted only as the final row).",
          "default": null
        }
      },
      "required": [
        "engageRate",
        "releaseRate"
      ],
      "additionalProperties": false
    },
    "MotionScalarEnvelope": {
      "type": [
        "object",
        "null"
      ],
      "properties": {
        "min": {
          "description": "The least admitted value (inclusive).",
          "type": "number"
        },
        "max": {
          "description": "The greatest admitted value (inclusive) \u2014 WorldDefinitionValidator refuses Max \u003C Min by name. Equal to Min pins the scalar outright regardless of what a profile requests.",
          "type": "number"
        }
      },
      "additionalProperties": false
    },
    "ActionEffectSetVerticalVelocity": {
      "description": "Writes the body\u0027s vertical-velocity channel (the jump launch / the surge). Under the grounded model gravity owns its decay; under the free model it bleeds to zero at the tuning\u0027s rise gravity (no fall phase).",
      "properties": {
        "$type": {
          "const": "setVerticalVelocity"
        },
        "velocity": {
          "type": "number"
        },
        "target": {
          "enum": [
            "Self",
            "ProducerTarget",
            "AffectingSubject"
          ],
          "default": "Self"
        }
      },
      "required": [
        "velocity"
      ],
      "additionalProperties": false
    },
    "ActionEffectScaleVerticalVelocity": {
      "description": "Multiplies the body\u0027s vertical velocity (the jump cut; gate on Rising).",
      "properties": {
        "$type": {
          "const": "scaleVerticalVelocity"
        },
        "factor": {
          "type": "number"
        },
        "target": {
          "enum": [
            "Self",
            "ProducerTarget",
            "AffectingSubject"
          ],
          "default": "Self"
        }
      },
      "required": [
        "factor"
      ],
      "additionalProperties": false
    },
    "ActionEffectPlanarImpulse": {
      "description": "A timed planar velocity overlay (the dash): BodyDirection is rotated by the body\u0027s attitude at fire time and ridden at Speed for DurationSeconds, integrated through its own accumulator on top of the model\u0027s motion \u2014 integration itself is untouched.",
      "properties": {
        "$type": {
          "const": "planarImpulse"
        },
        "bodyDirection": {},
        "speed": {
          "type": "number"
        },
        "durationSeconds": {
          "type": "number"
        },
        "target": {
          "enum": [
            "Self",
            "ProducerTarget",
            "AffectingSubject"
          ],
          "default": "Self"
        }
      },
      "required": [
        "bodyDirection",
        "speed",
        "durationSeconds"
      ],
      "additionalProperties": false
    },
    "ActionEffectSetState": {
      "description": "Writes a named state cell \u2014 a kit counter slot at body scope, a state-section row\u0027s cell at world scope (see WorldRule).",
      "properties": {
        "$type": {
          "const": "setState"
        },
        "state": {
          "description": "The counter slot (body scope) or state row name (world scope).",
          "type": "string"
        },
        "value": {
          "description": "The literal value to write, or null when FromState spells a live operand to copy instead \u2014 world scope only, exactly one of the two is authored (refused by name when both or neither are present, the same duality CompareState\u0027s own comparand carries). Required (non-null) at body scope, where a live copy source is refused.",
          "type": [
            "number",
            "null"
          ],
          "default": null
        },
        "target": {
          "description": "The addressed entity \u2014 body scope only; a non-Self target is refused at world scope, where there is no entity to select.",
          "enum": [
            "Self",
            "ProducerTarget",
            "AffectingSubject"
          ],
          "default": "Self"
        },
        "key": {
          "description": "The cell inside State at world scope \u2014 null writes the row\u0027s slot cell, which a keyed row does not have (refused by name). Refused at body scope.",
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "fromState": {
          "description": "world scope only (refused at body scope, on the same terms as Value): another declared state-section row name, or one of WorldRuleFacts\u0027s reserved channels, read live at fire time and copied in place of an authored Value \u2014 the row that resets to another row\u0027s own current value (a shadow row mirroring a counter someone else advances), never only a standing literal. Resolved through the same operand walk CompareState\u0027s own ComparandState uses; mixing a fixed row into an int destination (or the reverse) is refused by name rather than coerced.",
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "fromKey": {
          "description": "The cell inside FromState, on the same (row, key) terms as Key. Refused when FromState names a reserved channel or is absent.",
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "valueSeconds": {
          "description": "world scope only (refused at body scope, on the same terms as Value and FromState \u2014 exactly one of the three is authored): an alternative to Value for a kind=int state row a companion CountdownState effect decrements once per simulation tick (a countdown/cooldown). Authored in seconds \u2014 a physical unit, not a tick count, so a world\u0027s rate can change without silently retuning every cooldown \u2014 and converted once at rule compile time to an exact whole engine-tick count via TryDurationEngineTicksExact, never re-derived at runtime and never rounded: a duration that is not an exact whole engine-tick count is refused rather than silently rounded away (DurationNotExactEngineTicks). Typed Decimal rather than float because JSON deserializes a number token to Decimal exactly (base-10, no binary-float intermediate), and most terminating decimals \u2014 the only ones an author can spell \u2014 have no exact binary float or fixed-point spelling either. See WorldRuleCompiler.",
          "type": [
            "number",
            "null"
          ],
          "default": null
        },
        "text": {
          "description": "world scope only: the literal a kind=text state row\u0027s cell takes \u2014 the fourth spelling beside Value/FromState/ValueSeconds, exactly one authored. Because every state-bound document value (a creation palette colour, a look-assignment row name) re-resolves on the write, this is how a rule restyles what a body wears.",
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      },
      "required": [
        "state"
      ],
      "additionalProperties": false
    },
    "ActionEffectAddState": {
      "description": "Adds to a named state cell \u2014 a kit counter slot at body scope, a state-section row\u0027s cell at world scope (see WorldRule).",
      "properties": {
        "$type": {
          "const": "addState"
        },
        "state": {
          "description": "The counter slot (body scope) or state row name (world scope).",
          "type": "string"
        },
        "value": {
          "description": "The literal addend, or null when FromState spells a live addend instead \u2014 see Value\u0027s remarks; the same value/from duality, required (non-null) at body scope.",
          "type": [
            "number",
            "null"
          ],
          "default": null
        },
        "target": {
          "description": "The addressed entity \u2014 body scope only.",
          "enum": [
            "Self",
            "ProducerTarget",
            "AffectingSubject"
          ],
          "default": "Self"
        },
        "key": {
          "description": "The cell inside State at world scope; refused at body scope.",
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "fromState": {
          "description": "world scope only \u2014 see FromState\u0027s remarks; here the addend is read live rather than the replacement.",
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "fromKey": {
          "description": "The cell inside FromState \u2014 see FromKey.",
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "valueSeconds": {
          "description": "world scope only \u2014 see ValueSeconds\u0027s remarks; here the converted tick count is the addend rather than the replacement.",
          "type": [
            "number",
            "null"
          ],
          "default": null
        }
      },
      "required": [
        "state"
      ],
      "additionalProperties": false
    },
    "ActionEffectCountdownState": {
      "description": "Decrements a world-state countdown by the current simulation step\u0027s engine-tick width, saturating at zero. world scope only: the destination must be a kind=int nonNegative=true row. Unlike an authored AddState constant, this effect consumes the runtime step width, so changing the world\u0027s authored tick rate never retunes the duration. When the remaining duration is shorter than one step, the computed decrement is exactly the remaining value; it reaches zero without asking the explicit-write door to admit a negative candidate.",
      "properties": {
        "$type": {
          "const": "countdownState"
        },
        "state": {
          "description": "The countdown state-row name.",
          "type": "string"
        },
        "key": {
          "description": "The cell inside State; null addresses its slot.",
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      },
      "required": [
        "state"
      ],
      "additionalProperties": false
    },
    "ActionEffectStartTimer": {
      "description": "Starts a named timer slot with an authored duration.",
      "properties": {
        "$type": {
          "const": "startTimer"
        },
        "state": {
          "type": "string"
        },
        "seconds": {
          "type": "number"
        },
        "target": {
          "enum": [
            "Self",
            "ProducerTarget",
            "AffectingSubject"
          ],
          "default": "Self"
        }
      },
      "required": [
        "state",
        "seconds"
      ],
      "additionalProperties": false
    },
    "ActionEffectDesignate": {
      "description": "Submits the selected subject into a named target register.",
      "properties": {
        "$type": {
          "const": "designate"
        },
        "register": {
          "description": "The authored target-register name.",
          "type": "string"
        },
        "target": {
          "description": "The subject source.",
          "enum": [
            "Self",
            "ProducerTarget",
            "AffectingSubject"
          ],
          "default": "AffectingSubject"
        }
      },
      "required": [
        "register"
      ],
      "additionalProperties": false
    },
    "ActionEffectGenerate": {
      "description": "Redraws a draw site (a state row declaring a WorldDraw) \u2014 the one effect admissible at both scopes, and the join that makes authored randomness and world rules one arc rather than two: a kit action, a world rule, and the world.generate console verb all reduce to composing the same WorldMutation.Generate and letting it drain through the ordinary tick boundary, so journal/undo cover a draw for free wherever it was fired from. This is also how a draw\u0027s moment is authored: a TickPeriod site redraws on an ordinary $tick-scheduled rule and an Event site on an event-gated one, so timing costs no mutation ordinal. At body scope the firing is staged during the body\u0027s advance and enqueued for the next tick\u0027s drain (an honestly- reported one-tick latency: this is the first ActionEffect to write the document rather than per-body state, so it is the first to pay the pipeline\u0027s own round trip).",
      "properties": {
        "$type": {
          "const": "generate"
        },
        "row": {
          "description": "The draw site\u0027s row name. One name, not a (source, destination) pair: a site\u0027s source is its own facet and a site is a scalar slot, so there is nothing else to address.",
          "type": "string"
        }
      },
      "required": [
        "row"
      ],
      "additionalProperties": false
    },
    "ActionEffectJudge": {
      "description": "Stages a rhythm-judge grading fact for the body whose trigger fired \u2014 the entity index, the judge window set, and the firing tick are collected during the body\u0027s advance (the same staged-output shape Generate uses) and drained by WorldServer.Step immediately after the body step, where they are graded against the world\u0027s musical clock. The grade is never computed here \u2014 this effect only names which judge row applies.",
      "properties": {
        "$type": {
          "const": "judge"
        },
        "judgeRef": {
          "description": "The declared judges row name (a puck.judge.v1 reference) whose hit windows grade the press.",
          "type": "string"
        }
      },
      "required": [
        "judgeRef"
      ],
      "additionalProperties": false
    },
    "WorldHudRect": {
      "description": "A normalized rect (origin top-left, Y down) \u2014 a WorldHudPanel\u0027s rect is in screen space [0, 1] \u00D7 [0, 1]; a WorldHudElement\u0027s rect is in its owning panel\u0027s local [0, 1] \u00D7 [0, 1] space.",
      "type": "object",
      "properties": {
        "x": {
          "description": "The rect\u0027s left edge, normalized.",
          "type": "number"
        },
        "y": {
          "description": "The rect\u0027s top edge, normalized.",
          "type": "number"
        },
        "width": {
          "description": "The rect\u0027s width, normalized \u2014 must be positive.",
          "type": "number"
        },
        "height": {
          "description": "The rect\u0027s height, normalized \u2014 must be positive.",
          "type": "number"
        }
      },
      "additionalProperties": false
    },
    "OverlayFact": {
      "description": "A presentation fact an overlay element\u0027s OverlayPredicate reads. Every fact is per local seat and never enters the simulation; a world-scope element (a hud.panels row) reads a fact as true when it holds for any joined local seat.",
      "enum": [
        "SeatInput",
        "PointerMotion",
        "WheelOpen",
        "ConsoleOpen",
        "SeatCameraApplication"
      ]
    },
    "OverlayPredicateNow": {
      "description": "The fact holds this frame.",
      "properties": {
        "$type": {
          "const": "now"
        },
        "fact": {
          "$ref": "#/$defs/OverlayFact"
        }
      },
      "required": [
        "fact"
      ],
      "additionalProperties": false
    },
    "OverlayPredicateRecently": {
      "description": "The fact held within the last WindowSeconds.",
      "properties": {
        "$type": {
          "const": "recently"
        },
        "fact": {
          "$ref": "#/$defs/OverlayFact",
          "description": "The fact whose recency is tested."
        },
        "windowSeconds": {
          "description": "How long after the fact last held the predicate still holds in full.",
          "type": "number"
        },
        "fadeSeconds": {
          "description": "How long after the window the predicate\u0027s PRESENCE eases from 1 to 0 \u2014 a surface reading presence rather than the boolean fades out instead of cutting, and the predicate still HOLDS (presence above 0) throughout the fade. 0 cuts at the window\u0027s end.",
          "type": "number",
          "default": 0
        }
      },
      "required": [
        "fact",
        "windowSeconds"
      ],
      "additionalProperties": false
    },
    "OverlayPredicateAll": {
      "description": "Every predicate holds.",
      "properties": {
        "$type": {
          "const": "all"
        },
        "predicates": {
          "$ref": "#/$defs/OverlayPredicateListNonNullable"
        }
      },
      "required": [
        "predicates"
      ],
      "additionalProperties": false
    },
    "OverlayPredicateList": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/OverlayPredicateNullable"
      }
    },
    "OverlayPredicateAny": {
      "description": "At least one predicate holds.",
      "properties": {
        "$type": {
          "const": "any"
        },
        "predicates": {
          "$ref": "#/$defs/OverlayPredicateList"
        }
      },
      "required": [
        "predicates"
      ],
      "additionalProperties": false
    },
    "OverlayPredicateNot": {
      "description": "The predicate does not hold.",
      "properties": {
        "$type": {
          "const": "not"
        },
        "predicate": {
          "$ref": "#/$defs/OverlayPredicate"
        }
      },
      "required": [
        "predicate"
      ],
      "additionalProperties": false
    },
    "OverlayPredicate": {
      "description": "An overlay element\u0027s visibility condition \u2014 the presentation twin of ActionPredicate, over OverlayFacts. Absent on an element means always visible.",
      "type": "object",
      "required": [
        "$type"
      ],
      "anyOf": [
        {
          "$ref": "#/$defs/OverlayPredicateNow"
        },
        {
          "$ref": "#/$defs/OverlayPredicateRecently"
        },
        {
          "$ref": "#/$defs/OverlayPredicateAll"
        },
        {
          "$ref": "#/$defs/OverlayPredicateAny"
        },
        {
          "$ref": "#/$defs/OverlayPredicateNot"
        }
      ]
    },
    "OverlayPredicateNullable": {
      "description": "An overlay element\u0027s visibility condition \u2014 the presentation twin of ActionPredicate, over OverlayFacts. Absent on an element means always visible.",
      "type": [
        "object",
        "null"
      ],
      "required": [
        "$type"
      ],
      "anyOf": [
        {
          "$ref": "#/$defs/OverlayPredicateNow"
        },
        {
          "$ref": "#/$defs/OverlayPredicateRecently"
        },
        {
          "$ref": "#/$defs/OverlayPredicateAll"
        },
        {
          "$ref": "#/$defs/OverlayPredicateAny"
        },
        {
          "$ref": "#/$defs/OverlayPredicateNot"
        }
      ]
    },
    "OverlayPredicateListNonNullable": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/OverlayPredicateNullable"
      }
    },
    "WorldHudStyleToken": {
      "description": "A WorldHudElement\u0027s color role \u2014 a curated authored subset of Puck.Overlays.OverlayColorRole (Puck.World.Schema must not reference Puck.Overlays; the renderer maps this token to the concrete role).",
      "enum": [
        "Primary",
        "Dim",
        "Accent",
        "Positive",
        "Warning",
        "Danger"
      ]
    },
    "WorldFrameSource": {
      "description": "The WorldScreenSource arms that produce a sampled frame \u2014 Camera, View, Probe, and Capture \u2014 carrying its own JsonPolymorphicAttribute over the SAME four discriminators (camera/view/probe/capture) their parent union uses, so a member declared as this narrower type round-trips the identical JSON a full WorldScreenSource screen row does. A WorldProbe socket is the driving consumer: it plugs one of these into each named input rather than the wider union, which would legally admit a decal/machine/console/qr/text/none source no probe kernel can sample.",
      "type": [
        "object",
        "null"
      ],
      "required": [
        "$type"
      ],
      "anyOf": [
        {
          "$ref": "#/$defs/WorldScreenSourceCamera"
        },
        {
          "$ref": "#/$defs/WorldScreenSourceView"
        },
        {
          "$ref": "#/$defs/WorldScreenSourceProbe"
        },
        {
          "$ref": "#/$defs/WorldScreenSourceCapture"
        }
      ]
    },
    "WorldHudElement": {
      "description": "One HUD element row inside a WorldHudPanel \u2014 a stable id (unique within the owning panel), its kind, its local rect, its color role, an authored literal string (meaningful for Text), and an optional binding into the closed HudBindingVocabulary (meaningful for Text and Gauge \u2014 a bound text element\u0027s live value replaces the authored literal; a bound gauge element\u0027s live value drives its fill; an unbound gauge draws empty).",
      "type": [
        "object",
        "null"
      ],
      "properties": {
        "id": {
          "description": "The element\u0027s stable id (unique within the owning panel \u2014 the world.row.set hud.panels/ .remove mutation address).",
          "type": "string"
        },
        "kind": {
          "description": "The element\u0027s rendered kind.",
          "enum": [
            "Rect",
            "Text",
            "Gauge",
            "Frame"
          ]
        },
        "rect": {
          "$ref": "#/$defs/WorldHudRect",
          "description": "The element\u0027s rect, normalized to the owning panel\u0027s local space."
        },
        "style": {
          "$ref": "#/$defs/WorldHudStyleToken",
          "description": "The element\u0027s color role."
        },
        "text": {
          "description": "The authored literal string a Text element draws when neither Binding nor Template is set; ignored for Rect and Gauge. Omitted from the wire when null.",
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "binding": {
          "description": "A closed HudBindingVocabulary token, or null for an unbound element. Refused alongside Template \u2014 exactly one live-value source, never both. Omitted from the wire when null.",
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "template": {
          "description": "A Text element\u0027s template string \u2014 authored literal text interleaved with {token} placeholders, each a closed HudBindingVocabulary token resolved through the same operand path Binding uses (see HudTemplate for the brace/escape grammar). A richer binding source than Binding \u2014 many live facts composed into one string instead of one \u2014 never both on the same element. Ignored for Rect and Gauge (a gauge\u0027s fill is one fraction; it has no composed string to show). Omitted from the wire when null.",
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "source": {
          "$ref": "#/$defs/WorldFrameSource",
          "description": "A Frame element\u0027s sampled frame \u2014 the same WorldFrameSource vocabulary a screen row or probe socket plugs into (camera/view/probe/capture). Required for Frame; refused on every other kind. Omitted from the wire when null.",
          "default": null
        },
        "fit": {
          "description": "A Frame element\u0027s aspect-fit policy. Ignored for every other kind. Omitted from the wire at its default (Cover).",
          "enum": [
            "Cover",
            "Contain",
            "Stretch"
          ],
          "default": "Cover"
        },
        "mirror": {
          "description": "Whether a Frame element flips its sampled frame horizontally \u2014 a face cam is conventionally mirrored. Ignored for every other kind. Omitted from the wire at its default (false).",
          "type": "boolean",
          "default": false
        },
        "radius": {
          "description": "A Frame element\u0027s corner-rounding radius, in pixels. Must be finite and non-negative. Ignored for every other kind. Omitted from the wire at its default (0).",
          "type": "number",
          "default": 0
        },
        "opacity": {
          "description": "A Frame element\u0027s sampled-frame alpha, in [0, 1]. Ignored for every other kind.",
          "type": "number",
          "default": 1
        }
      },
      "required": [
        "id",
        "kind",
        "rect",
        "style"
      ],
      "additionalProperties": false
    },
    "WorldHudElementList": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/WorldHudElement"
      }
    },
    "WorldHudPanel": {
      "description": "One HUD panel row \u2014 a stable id (unique within the section), a normalized viewport rect in screen space, which band it draws in, its chrome style, and its child elements. WorldMutation.UpsertHudPanel carries the whole row (elements included) as one cross-row transaction boundary; WorldMutation.UpsertHudElement/ WorldMutation.RemoveHudElement read-modify-write a single element within an already-declared panel.",
      "type": "object",
      "properties": {
        "id": {
          "description": "The panel\u0027s stable id (unique within the section \u2014 the world.row.set hud.panels/world.row.remove hud.panels mutation address).",
          "type": "string"
        },
        "rect": {
          "$ref": "#/$defs/WorldHudRect",
          "description": "The panel\u0027s viewport rect, normalized to screen space."
        },
        "layer": {
          "description": "Which band the panel draws in.",
          "enum": [
            "Under",
            "Over",
            "Replace"
          ]
        },
        "style": {
          "description": "The panel\u0027s chrome recipe.",
          "enum": [
            "Panel",
            "Strip",
            "Chip"
          ]
        },
        "visible": {
          "$ref": "#/$defs/OverlayPredicateNullable",
          "description": "The panel\u0027s visibility condition over presentation facts, or null for always.",
          "default": null
        },
        "elements": {
          "$ref": "#/$defs/WorldHudElementList",
          "description": "Gets the panel\u0027s child elements. The absence-coalesce lives in the accessor for the same reason Response\u0027s does."
        }
      },
      "required": [
        "id",
        "rect",
        "layer",
        "style",
        "elements"
      ],
      "additionalProperties": false
    },
    "ActionEffectUpsertHudPanel": {
      "description": "Upserts a whole HUD panel row \u2014 world scope only (refused at body scope: a per-body action has no HUD panel of its own to author). Admits WorldMutation.UpsertHudPanel into the world-rule effect set through the same seam Generate uses: the compiled effect submits the mutation stamped World, which WorldServer.TryAdmitMutation admits structurally, so the panel\u0027s own validation (capacity, unknown binding) is the ordinary whole-document revalidation every UpsertHudPanel submission \u2014 console, addon, or rule \u2014 already passes through.",
      "properties": {
        "$type": {
          "const": "upsertHudPanel"
        },
        "panel": {
          "$ref": "#/$defs/WorldHudPanel",
          "description": "The whole panel row, elements included."
        }
      },
      "required": [
        "panel"
      ],
      "additionalProperties": false
    },
    "ActionEffectRemoveHudPanel": {
      "description": "Removes a HUD panel row by id \u2014 world scope only. See UpsertHudPanel\u0027s remarks.",
      "properties": {
        "$type": {
          "const": "removeHudPanel"
        },
        "id": {
          "description": "The panel id to remove.",
          "type": "string"
        }
      },
      "required": [
        "id"
      ],
      "additionalProperties": false
    },
    "WorldPlacementMirror": {
      "description": "A reflection plane in a placement\u0027s local frame.",
      "type": [
        "object",
        "null"
      ],
      "properties": {
        "normal": {
          "description": "The plane normal."
        },
        "offset": {
          "description": "The signed plane offset along the normalized Normal.",
          "type": "number"
        }
      },
      "required": [
        "normal",
        "offset"
      ],
      "additionalProperties": false
    },
    "WorldEmission": {
      "description": "An emission facet \u2014 a synth voice a world row itself makes (phenomena sound like themselves; a creek is not a speaker). Nullable on WorldPlacement \u2014 a facet edit is the row\u0027s existing whole-row upsert. Under a repeat facet the emission binds to the placement root only (an 8\u00D78 lattice must not become 64 voices; a per-copy flag is a future facet field, not a schema fork).",
      "type": [
        "object",
        "null"
      ],
      "properties": {
        "patchId": {
          "description": "The referenced Id (must resolve).",
          "type": "string"
        },
        "level": {
          "description": "The emitter level (1 = unity), bounded by MaxLevel.",
          "type": "number"
        },
        "radius": {
          "description": "The audible support radius in world units, or null for the audio defaults\u0027 DefaultSpeakerRadius.",
          "type": [
            "number",
            "null"
          ],
          "default": null
        }
      },
      "required": [
        "patchId",
        "level"
      ],
      "additionalProperties": false
    },
    "WorldPlacementInhabit": {
      "description": "A placement\u0027s inhabit facet \u2014 the row\u0027s binding to live population bodies. An inhabited placement is a normal entry in the entity table: it holds a Puck.World.Server.WorldBody, integrates under the named kit, and is addressable as Entity like any avatar. Its stamp rides the body\u0027s pose instead of the row\u0027s static transform; the row\u0027s position/yaw become its spawn pose. Absent (null) = decoration, the unchanged furniture behaviour.",
      "type": [
        "object",
        "null"
      ],
      "properties": {
        "kit": {
          "description": "The Name the bodies move under. Null resolves the creation\u0027s own Locomotion token AS a kit name \u2014 a creation declaring \u0022swim\u0022 inhabits the world\u0027s kit row named \u0022swim\u0022. Neither resolving is a loud rejection naming every kit the world declares.",
          "type": [
            "string",
            "null"
          ]
        },
        "look": {
          "description": "The Name the bodies wear, or null to wear an implicit creation look on this placement\u0027s own CreationId.",
          "type": [
            "string",
            "null"
          ]
        },
        "source": {
          "description": "The live, idle, or named producer source the bodies wake on."
        },
        "count": {
          "description": "How many bodies, bounded by the world\u0027s authored peer capacity.",
          "type": "integer",
          "default": 1
        },
        "distribution": {
          "$ref": "#/$defs/WorldDistribution",
          "description": "The region and deterministic fill sequence that place the bodies relative to the placement root.",
          "default": null
        }
      },
      "required": [
        "kit",
        "look",
        "source"
      ],
      "additionalProperties": false
    },
    "WorldPlacementPortal": {
      "description": "A WorldPlacementFace\u0027s portal facet \u2014 the authored decision that a face is a door: which WorldDestination row it leads to, and under what travel scope. Absent (the default) means the face is not a door \u2014 nothing here fires anything by itself; turning the decision into a diegetic step-into trigger is WorldInstanceHost.TriggerPortal\u0027s job, never this facet\u0027s. Durability, scope, and process-local instance selection live on the named WorldDestination row this facet points at, not here \u2014 a facet composes one destination selection with a travel scope, never re-authors how that destination is minted. Extensible deliberately (an optional-member record, the same widen-without-moving-existing-members shape WorldWaterSection\u0027s own remarks describe): a future fact-gate field (an authored predicate a traveler must satisfy to pass) adds cleanly as a new trailing member.",
      "type": [
        "object",
        "null"
      ],
      "properties": {
        "destination": {
          "description": "The Name of the destinations row naming the selected destination. Must resolve to an existing row \u2014 an undeclared name refuses by name (see WorldDefinitionValidator). No boot-time file-existence check on the destination\u0027s own referenced document \u2014 resolving the document is a future consumer\u0027s job; resolving the name, against this document\u0027s own destinations section, is this facet\u0027s own.",
          "type": "string"
        },
        "travel": {
          "description": "Who travels through (see WorldPortalTravel). Absent resolves against the world\u0027s own portals.portalDefaults.travel (see WorldPortalsSection), or Body when the world declares no portals section at all \u2014 see WorldDefinitionValidator\u0027s resolution order, echoed by world.portals. Omitted from the wire when null.",
          "default": null,
          "type": [
            "string",
            "null"
          ],
          "enum": [
            "party",
            "body",
            null
          ]
        },
        "arrival": {
          "description": "Where a traveler lands at the destination (see WorldPortalArrival). Default Spawn \u2014 unauthored worlds and every facet authored before this member existed are unchanged. Optional and trailing (the same widen-without-moving-existing-members shape Travel itself already follows).",
          "default": "spawn",
          "type": "string",
          "enum": [
            "spawn",
            "mapped"
          ]
        },
        "counterpart": {
          "description": "The destination document\u0027s border face this facet\u0027s frame maps onto under Mapped \u2014 \u0022\u003CplacementId\u003E/\u003Cface\u003E\u0022, the placement id and declared face name whose own placement transform (position \u002B yaw) is the arrival anchor. Required exactly when Arrival is Mapped (refused by name otherwise, in either direction \u2014 see WorldDefinitionValidator). No boot-time cross-document existence check: the named document is not resolved at boot (same reason Destination is not), so an absent placement/face refuses at transfer time instead, against the destination\u0027s own delivered definition (see WorldPortalCounterpart). Omitted from the wire when null.",
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "capacity": {
          "description": "The maximum number of live bodies and outstanding reservations admitted through this border at once, or null to use the destination population\u0027s remaining capacity. A full border refuses the current attempt immediately; it never queues.",
          "type": [
            "integer",
            "null"
          ],
          "default": null
        }
      },
      "required": [
        "destination"
      ],
      "additionalProperties": false
    },
    "WorldPlacementFace": {
      "description": "A per-instance override of one declared creation face\u0027s feed \u2014 the face twin of the emission facet\u0027s per-instance override channel.",
      "type": [
        "object",
        "null"
      ],
      "properties": {
        "face": {
          "description": "The declared Name to override.",
          "type": "string"
        },
        "source": {
          "$ref": "#/$defs/WorldScreenSource",
          "description": "The screen source the face shows, in the existing WorldScreenSource vocabulary."
        },
        "portal": {
          "$ref": "#/$defs/WorldPlacementPortal",
          "description": "The face\u0027s portal facet (see WorldPlacementPortal) \u2014 absent (the default) means this face is not a door. Optional and trailing deliberately: a face authored before this facet existed round-trips unchanged, and it composes freely with Source \u2014 the door and the screen it shows are independent facts about the same face.",
          "default": null
        }
      },
      "required": [
        "face",
        "source"
      ],
      "additionalProperties": false
    },
    "WorldPlacementFaceList": {
      "type": [
        "array",
        "null"
      ],
      "items": {
        "$ref": "#/$defs/WorldPlacementFace"
      }
    },
    "WorldPlacementRegion": {
      "description": "A placement\u0027s region facet \u2014 a named volume row, not a trigger system: any placement may carry one, turning its stamp into a sensing volume the world-events feed watches for body enter/exit edges (see Server.WorldEventFeed and the observe region:\u003Cname\u003E grant subject). The region\u0027s name is the carrying placement\u0027s Id \u2014 one identity, never a second string kept in sync by hand. The volume is a sphere centered on the placement\u0027s Position (the placement\u0027s own Scale/YawDegrees do not affect it \u2014 a region\u0027s size is its own authored radius, never derived from the creation\u0027s visual bounds). Presentation-only in itself (drawing no geometry); sensing reads the same document-authored center every tick, converted to fixed-point at the same boundary WorldSolid facets already cross through \u2014 unless the row also carries Attach, in which case the center is the resolved live body pose instead (Server.WorldEventFeed.CollectRegions, the same resolve world.attachments answers): the sensing sphere follows the carrier, and an inactive carrier senses nobody rather than sensing at a stale point.",
      "type": [
        "object",
        "null"
      ],
      "properties": {
        "radius": {
          "description": "The sensing radius, world units. Must be finite and positive (validated).",
          "type": "number"
        }
      },
      "required": [
        "radius"
      ],
      "additionalProperties": false
    },
    "WorldPlacementAttach": {
      "description": "A placement\u0027s attach facet \u2014 binds the row\u0027s stamp to a live population body\u0027s transform, so the resolved world pose follows that body every tick (an avatar\u0027s hat, held item, nameplate, or aura) instead of sitting at the row\u0027s own authored Position/YawDegrees. The offset rides the body\u0027s own local frame \u2014 rotated by the body\u0027s orientation before adding, the Puck.SdfVm.Views.OrientedFollowRig/FirstPersonRig convention for a moving anchor, never the world-axis FollowRig shape a fixed subject would use. The resolved pose is never written back into the document, and it is derived twice, at two clocks, from the one authored facet: the authoritative answer is fixed point \u2014 the body\u0027s fixed-point pose composed with this facet\u0027s authored (float, quantized at resolution like every other placement field) offset, by Puck.World.Server.WorldPlacementAttachment.TryResolve, on demand: world.attachments is its only caller today, so it runs when a reader asks rather than on a schedule;the rendered pose is presentation float \u2014 the same composition over the client\u0027s interpolated body pose, packed every frame by Client.WorldStampPool, which is what makes an attached row visibly ride its body as smoothly as the body itself. An attached row draws through that reserved stamp pool and not as a static stamp (Client.WorldPlacementStamper.IsStaticStamp), and it charges MaxStampRegistrations like an animated row does. Region, solid (under the analytic contact provider), and emission were once refused on the same row as this one because each read the row\u0027s own static transform \u2014 all three now read the same resolved dynamic pose instead (Server.WorldEventFeed.CollectRegions, Server.WorldColliderSet.RefreshAttached, Client.WorldStampPool.TryShapePosition/RootPose), so a region\u0027s aura, an analytic collider\u0027s hitbox, and an emission\u0027s voice all track the carrier: an equipped item\u0027s sensing sphere, hitbox, or source point rides the body it is attached to. What stays refused: distribution/mirror (static-stamp-only, the same rule an animated or inhabited row already enforces), inhabit (a row cannot both spawn its own driven bodies and ride another\u0027s), and solid specifically under the field contact provider (it compiles every solid row\u0027s geometry once into one SDF program and never rebuilds it per tick) \u2014 refused by name rather than defining a blend (see WorldDefinitionValidator).",
      "type": [
        "object",
        "null"
      ],
      "properties": {
        "bodyIndex": {
          "description": "The 0-based population entity index the placement rides \u2014 the same indexing Entity and the console\u0027s body:\u003Cn\u003E grant subject use, not the 1-based player.* seat number (body:1 is \u0022player 2\u0022). Validated within 0..the world\u0027s authored population capacity; the target need not be active at author time (see remarks \u2014 an inactive body at runtime makes the row contribute nothing, it does not refuse).",
          "type": "integer"
        },
        "localOffset": {
          "description": "The stamp\u0027s position offset in the body\u0027s own local frame, world units."
        },
        "localYawDegrees": {
          "description": "The stamp\u0027s yaw offset from the body\u0027s own heading, degrees. Zero rides the body\u0027s exact facing.",
          "type": "number",
          "default": 0
        }
      },
      "required": [
        "bodyIndex",
        "localOffset"
      ],
      "additionalProperties": false
    },
    "WorldPlacementContribution": {
      "description": "A placement\u0027s contribution facet: the row is a slot whose frame (id, pose, scale, and these lifecycle terms) is host-authored and whose creation a federation partner supplies through ordinary mutations. Null is an ordinary placement.",
      "type": [
        "object",
        "null"
      ],
      "properties": {
        "tenure": {
          "description": "The authored lifecycle.",
          "enum": [
            "Presence",
            "Endowed"
          ]
        },
        "slotCreationId": {
          "description": "The host-owned Id the slot shows while unfilled and returns to on retraction. Must resolve to a declared creation row.",
          "type": "string"
        },
        "link": {
          "description": "The authored adjacencies row name the presence check watches \u2014 the same key the $link:\u003CadjacencyName\u003E reserved rule channel reads. Required for Presence, refused beside Endowed.",
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "graceSeconds": {
          "description": "Seconds the watched link may stay unreachable before the piece retracts, compiled to simulation ticks through CompiledGrace at each sweep observation and stamped once into RetractDeadlineTick. Zero disables the grace (retract on the first observed outage); a rate-0 world compiles to Never, so nothing retracts. Refused beside Endowed unless zero.",
          "type": "number",
          "default": 0
        },
        "contributor": {
          "description": "Server-stamped. The acting principal that filled the slot; null while unfilled. Never accepted from a submitted payload or an authored document.",
          "default": null
        },
        "retractDeadlineTick": {
          "description": "Server-stamped. The simulation tick at or after which the presence sweep retracts this slot; null while the link is reachable.",
          "type": [
            "integer",
            "null"
          ],
          "default": null
        }
      },
      "required": [
        "tenure",
        "slotCreationId"
      ],
      "additionalProperties": false
    },
    "WorldPlacement": {
      "description": "One placement instance row \u2014 a creation asset stamped into the world by reference: transform \u002B facets as data, addressed by its stable Id. A placement whose creation carries timeline frames is animated: it replays client-side on the render clock through the reserved dynamic-transform pool (distribution/mirror facets are static-stamp-only and reject on an animated row). A placement carrying an Inhabit facet is a live population body rather than furniture (see WorldPlacementInhabit); its declared creation eyes derive WorldCamera feeds and its declared faces derive screens (both at the delivery boundary, never written to the document).",
      "type": "object",
      "properties": {
        "id": {
          "description": "The row\u0027s stable string id (its mutation address).",
          "type": "string"
        },
        "creationId": {
          "description": "The referenced Id (must resolve; removal of a referenced creation rejects loudly).",
          "type": "string"
        },
        "position": {
          "description": "The stamp position, world space. Inert (still validated and stored, but read by nothing \u2014 neither the resolve nor the renderer) when Attach is set: the row\u0027s live position is the resolved attachment, never this authored one."
        },
        "yawDegrees": {
          "description": "The stamp yaw about \u002BY, degrees. Same attach caveat as Position.",
          "type": "number"
        },
        "scale": {
          "description": "The uniform stamp scale (clamped to the placement policy envelope by validation).",
          "type": "number"
        },
        "distribution": {
          "$ref": "#/$defs/WorldDistribution",
          "description": "The placement distribution, or null for a single copy. Static placements currently accept a lattice region with a none fill. Refused together with Attach.",
          "default": null
        },
        "mirror": {
          "$ref": "#/$defs/WorldPlacementMirror",
          "description": "The authored local reflection plane, or null for no reflected copy. Refused together with Attach.",
          "default": null
        },
        "emission": {
          "$ref": "#/$defs/WorldEmission",
          "description": "The placement\u0027s emission facet (a synth voice the stamp itself makes \u2014 see WorldEmission), or null for silent. Under Distribution the emission binds to the placement root only. Omitted from the wire when null. Composes with Attach: an attached row\u0027s source point rides the resolved live pose (Client.WorldStampPool.TryShapePosition) instead of the row\u0027s static position, and an inactive carrier silences the emitter rather than leaving it at a stale point.",
          "default": null
        },
        "solid": {
          "$ref": "#/$defs/WorldSolid",
          "description": "The placement\u0027s solidity facet (see WorldSolid). Both contact providers compile the creation\u0027s emitted shapes; analytic collision uses per-primitive colliders, including exact half-spaces for planes. Omitted from the wire when null. Composes with Attach under the analytic provider only (WorldColliderSet.RefreshAttached recomputes an attached row\u0027s colliders every tick from the resolved live pose); still refused together under the field provider, which compiles every solid row\u0027s geometry once into one SDF program and never rebuilds it per tick.",
          "default": null
        },
        "inhabit": {
          "$ref": "#/$defs/WorldPlacementInhabit",
          "description": "The inhabit facet (null = decoration), binding the row to live population bodies. Omitted from the wire when null. Refused together with Attach \u2014 a row cannot both spawn its own driven bodies and ride another body\u0027s pose.",
          "default": null
        },
        "faceSources": {
          "$ref": "#/$defs/WorldPlacementFaceList",
          "description": "Per-instance overrides of the creation\u0027s declared faces (null = every face shows its declared default). Omitted from the wire when null. Orthogonal to Attach (a content selector, not a transform) \u2014 composes freely, like every other facet that now tracks the dynamic pose.",
          "default": null
        },
        "region": {
          "$ref": "#/$defs/WorldPlacementRegion",
          "description": "The placement\u0027s region facet (see WorldPlacementRegion) \u2014 a named volume the world-events feed watches for body enter/exit, or null for none. Omitted from the wire when null. Composes with Attach: an attached row\u0027s sensing sphere centers on the resolved live pose (Server.WorldEventFeed.CollectRegions) instead of the row\u0027s static position \u2014 see WorldPlacementRegion\u0027s own remarks.",
          "default": null
        },
        "attach": {
          "$ref": "#/$defs/WorldPlacementAttach",
          "description": "The placement\u0027s attach facet (see WorldPlacementAttach) \u2014 binds the row\u0027s resolved world pose to a live population body, or null for a static/authored transform (the default, unchanged behavior). Omitted from the wire when null.",
          "default": null
        },
        "contribution": {
          "$ref": "#/$defs/WorldPlacementContribution",
          "description": "The placement\u0027s contribution facet (see WorldPlacementContribution) \u2014 marks the row a host-authored slot a federation partner fills, or null for an ordinary placement. Omitted from the wire when null. Composes with every other facet: the facet governs which creation the row shows and for how long, never its transform.",
          "default": null
        }
      },
      "required": [
        "id",
        "creationId",
        "position",
        "yawDegrees",
        "scale"
      ],
      "additionalProperties": false
    },
    "ActionEffectUpsertPlacement": {
      "description": "Upserts a whole placement row \u2014 world scope only (refused at body scope: a per-body action has no placement of its own to author). Admits WorldMutation.UpsertPlacement into the world-rule effect set through the same seam Generate uses.",
      "properties": {
        "$type": {
          "const": "upsertPlacement"
        },
        "placement": {
          "$ref": "#/$defs/WorldPlacement",
          "description": "The whole placement row."
        }
      },
      "required": [
        "placement"
      ],
      "additionalProperties": false
    },
    "ActionEffectRemovePlacement": {
      "description": "Removes a placement row by id \u2014 world scope only. See UpsertPlacement\u0027s remarks.",
      "properties": {
        "$type": {
          "const": "removePlacement"
        },
        "id": {
          "description": "The placement id to remove.",
          "type": "string"
        }
      },
      "required": [
        "id"
      ],
      "additionalProperties": false
    },
    "ActionEffectSave": {
      "description": "Writes a session snapshot of the world to its own loaded file \u2014 world scope only (refused at body scope: a per-body action has no world file of its own to save). A rule gate now decides when a save happens (an every-N-ticks cadence, a boss-defeated edge), closing the one gap the mutation substrate could not: a rule could already express any cadence over $tick or a state fact, but had nothing to fire that composed a save \u2014 every prior save was a human typing world.save, so a crashed server rewound to the last manual one.",
      "properties": {
        "$type": {
          "const": "save"
        }
      },
      "additionalProperties": false
    },
    "ActionEffectPose": {
      "description": "Teleports one body to a pose \u2014 the rule-side spelling of the player.pose verb, world scope only (refused at body scope). Like Save it submits no WorldMutation: a pose is body state, not document state, so nothing composes, validates, or journals, and a replay reproduces it by re-firing the same rule. Applied as the world\u0027s own act through WorldBody.Pose, deliberately outside the drive-admission gate: a body whose gatesDrive row reads nonzero is a body a rule still needs to move (a dead body back to its spawn). The client sees the same teleport continuity a player.pose produces.",
      "properties": {
        "$type": {
          "const": "pose"
        },
        "key": {
          "description": "The target body\u0027s 0-based entity index, spelled as a plain integer \u2014 the same literal addressing every other world-scope effect uses for a per-body cell.",
          "type": "string"
        },
        "spawnPoint": {
          "description": "A declared spawnPoints id (position and yaw from the point; pitch/roll zero). Exactly one of this and Position is authored.",
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "position": {
          "description": "A literal world position. Exactly one of this and SpawnPoint is authored.",
          "default": null
        },
        "yawDegrees": {
          "description": "The yaw about \u002BY, degrees; read only with Position.",
          "type": "number",
          "default": 0
        },
        "pitchDegrees": {
          "description": "The pitch about the body right, degrees; read only with Position.",
          "type": "number",
          "default": 0
        },
        "rollDegrees": {
          "description": "The roll about the body forward, degrees; read only with Position.",
          "type": "number",
          "default": 0
        }
      },
      "required": [
        "key"
      ],
      "additionalProperties": false
    },
    "ActionEffect": {
      "description": "An authored operand row lowered to a BodyMotionOp and executed by the body instruction interpreter when its trigger fires.",
      "type": [
        "object",
        "null"
      ],
      "required": [
        "$type"
      ],
      "anyOf": [
        {
          "$ref": "#/$defs/ActionEffectSetVerticalVelocity"
        },
        {
          "$ref": "#/$defs/ActionEffectScaleVerticalVelocity"
        },
        {
          "$ref": "#/$defs/ActionEffectPlanarImpulse"
        },
        {
          "$ref": "#/$defs/ActionEffectSetState"
        },
        {
          "$ref": "#/$defs/ActionEffectAddState"
        },
        {
          "$ref": "#/$defs/ActionEffectCountdownState"
        },
        {
          "$ref": "#/$defs/ActionEffectStartTimer"
        },
        {
          "$ref": "#/$defs/ActionEffectDesignate"
        },
        {
          "$ref": "#/$defs/ActionEffectGenerate"
        },
        {
          "$ref": "#/$defs/ActionEffectJudge"
        },
        {
          "$ref": "#/$defs/ActionEffectUpsertHudPanel"
        },
        {
          "$ref": "#/$defs/ActionEffectRemoveHudPanel"
        },
        {
          "$ref": "#/$defs/ActionEffectUpsertPlacement"
        },
        {
          "$ref": "#/$defs/ActionEffectRemovePlacement"
        },
        {
          "$ref": "#/$defs/ActionEffectSave"
        },
        {
          "$ref": "#/$defs/ActionEffectPose"
        }
      ]
    },
    "ActionEffectList": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/ActionEffect"
      }
    },
    "ActionTrigger": {
      "description": "One trigger channel of a lane binding: a gate, a press latch (the buffer \u2014 a press stays pending until the gate opens or the latch expires; the release channel latches nothing), and the effects a fire applies in order.",
      "type": [
        "object",
        "null"
      ],
      "properties": {
        "effects": {
          "$ref": "#/$defs/ActionEffectList",
          "description": "The effects applied on fire, in order."
        },
        "gate": {
          "$ref": "#/$defs/ActionPredicate",
          "description": "The predicate that must hold to fire, or null for always.",
          "default": null
        },
        "latchSeconds": {
          "description": "How long a press stays pending waiting for the gate. 0 means this tick only \u2014 the press fires if the gate is open on its own edge tick and is dropped otherwise. Legitimate only on OnPress: the release channel latches nothing, so a non-zero value on OnRelease is refused by name at validation rather than parsed and discarded.",
          "type": "number",
          "default": 0
        }
      },
      "required": [
        "effects"
      ],
      "additionalProperties": false
    },
    "DocumentIdentifierList": {
      "type": "array"
    },
    "WorldRowAssignment": {
      "description": "The row-to-entity assignment declaration \u2014 nothing about Sequence/Rows is kit-specific, so the same primitive distributes the kit table (a way of moving) and the look table (a way of looking) across the population. Resolved once at construction into each entry\u0027s fixed row index (precompute; zero steady-state cost). The kit assignment affects the simulation (it selects the compiled tuning/action bindings); the look assignment is presentation-only (it selects the appearance row).",
      "type": [
        "object",
        "null"
      ],
      "properties": {
        "sequence": {
          "$ref": "#/$defs/WorldSequence",
          "description": "The sequence that selects a row."
        },
        "rows": {
          "$ref": "#/$defs/DocumentIdentifierList",
          "description": "Gets the authored row-name view. The absence-coalesce lives in the accessor for the same reason Response\u0027s does."
        }
      },
      "required": [
        "sequence",
        "rows"
      ],
      "additionalProperties": false
    },
    "BindingActivatorDefinition": {
      "type": [
        "object",
        "null"
      ],
      "properties": {
        "sequence": {
          "type": "array",
          "items": {
            "type": [
              "string",
              "null"
            ]
          }
        },
        "mode": {
          "enum": [
            "Held",
            "Tapped"
          ],
          "default": "Held"
        },
        "timeoutTicks": {
          "type": [
            "integer",
            "null"
          ],
          "default": null
        }
      },
      "required": [
        "sequence"
      ],
      "additionalProperties": false
    },
    "BindingPageEntryDefinition": {
      "type": [
        "object",
        "null"
      ],
      "properties": {
        "sources": {
          "type": [
            "array",
            "null"
          ],
          "items": {
            "type": [
              "string",
              "null"
            ]
          }
        },
        "command": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "channel": {
          "default": null
        },
        "scale": {
          "type": [
            "number",
            "null"
          ],
          "default": null
        },
        "activateOn": {
          "enum": [
            "Started",
            "Active",
            "Completed",
            "Canceled",
            null
          ],
          "default": null
        },
        "label": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "id": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "value": {
          "default": null
        },
        "text": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "activator": {
          "$ref": "#/$defs/BindingActivatorDefinition",
          "default": null
        },
        "mode": {
          "enum": [
            "Hold",
            "Toggle"
          ],
          "default": "Hold"
        }
      },
      "required": [
        "sources"
      ],
      "additionalProperties": false
    },
    "BindingPageEntryDefinitionList": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/BindingPageEntryDefinition"
      }
    },
    "BindingPageDefinition": {
      "type": [
        "object",
        "null"
      ],
      "properties": {
        "id": {
          "type": "string"
        },
        "entries": {
          "$ref": "#/$defs/BindingPageEntryDefinitionList"
        },
        "label": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "icon": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "inherits": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      },
      "required": [
        "id",
        "entries"
      ],
      "additionalProperties": false
    },
    "WorldSpeakerSourceMachine": {
      "description": "A live screen-hosted machine\u0027s audio, identified by screen slot \u2014 screen index is machine identity for screen-hosted machines. The validator checks only that the screen row exists, never that its declared source is $type machine (runtime inserts overlay declared sources); no live machine at drain time is silence plus a state echo, never a reject.",
      "properties": {
        "$type": {
          "const": "machine"
        },
        "screenIndex": {
          "description": "The declared Index whose hosted machine feeds this source.",
          "type": "integer"
        }
      },
      "required": [
        "screenIndex"
      ],
      "additionalProperties": false
    },
    "WorldSpeakerSourceTune": {
      "description": "A tune asset (WorldTune) played through a headless machine host \u2014 acquired while any speaker references it, released when orphaned (a runtime derivation, never a data concept).",
      "properties": {
        "$type": {
          "const": "tune"
        },
        "tuneId": {
          "description": "The referenced Id (must resolve).",
          "type": "string"
        }
      },
      "required": [
        "tuneId"
      ],
      "additionalProperties": false
    },
    "WorldSpeakerSourceSynth": {
      "description": "The world voice synth playing a patch asset (WorldPatch). Patches are mono by construction: the feed\u0027s channel selectors degenerate to mix \u2014 documented, never rejected.",
      "properties": {
        "$type": {
          "const": "synth"
        },
        "patchId": {
          "description": "The referenced Id (must resolve).",
          "type": "string"
        }
      },
      "required": [
        "patchId"
      ],
      "additionalProperties": false
    },
    "WorldSpeakerSource": {
      "description": "The signal a WorldSpeaker\u0027s feed taps \u2014 a shared source identity, never an inline payload: the runtime drains each distinct source once per mix block and every feed tapping it shares that one pull, so \u0022stereo = two rows sharing a source\u0022 costs one drain. The $type string is the JSON discriminator, matching WorldScreenSource\u0027s convention; a new source kind is a new derived record plus its JsonDerivedTypeAttribute line.",
      "type": "object",
      "required": [
        "$type"
      ],
      "anyOf": [
        {
          "$ref": "#/$defs/WorldScreenSourceNone",
          "description": "No signal is bound \u2014 honest silence (the emitter holds its place; audio.emitters reads the state)."
        },
        {
          "$ref": "#/$defs/WorldSpeakerSourceMachine"
        },
        {
          "$ref": "#/$defs/WorldSpeakerSourceTune"
        },
        {
          "$ref": "#/$defs/WorldSpeakerSourceSynth"
        }
      ]
    },
    "WorldSpeakerFeed": {
      "description": "A speaker\u0027s feed \u2014 what it plays: a shared source identity, a stereo channel selector, and a gain. Stereo separation is two independent speaker rows sharing one source with left/right selectors and different geometry \u2014 no group/attachment construct. Mono sources (the synth) degenerate every selector to ChannelMix.",
      "type": "object",
      "properties": {
        "source": {
          "$ref": "#/$defs/WorldSpeakerSource",
          "description": "The shared source identity this feed taps."
        },
        "channel": {
          "description": "The stereo channel selector \u2014 ChannelMix, ChannelLeft, or ChannelRight.",
          "type": "string"
        },
        "gain": {
          "description": "The feed gain (1 = unity), bounded by MaxLevel \u2014 the one audio gain ceiling the validator enforces everywhere.",
          "type": "number"
        }
      },
      "required": [
        "source",
        "channel",
        "gain"
      ],
      "additionalProperties": false
    },
    "WorldSpeakerAttenuation": {
      "description": "A point speaker\u0027s distance-attenuation policy, or null on the row to coalesce to the WorldAudioDefaults section (DefaultSpeakerRadius/DefaultCurve).",
      "type": [
        "object",
        "null"
      ],
      "properties": {
        "radius": {
          "description": "The finite audible support radius in world units \u2014 at or beyond it the emitter is culled (finite support is the cull).",
          "type": "number"
        },
        "curve": {
          "description": "The falloff curve token (CurveSmoothstep or CurveLinear), or null for the audio-defaults curve.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "radius",
        "curve"
      ],
      "additionalProperties": false
    },
    "ActionFactNullable": {
      "enum": [
        "Grounded",
        "Airborne",
        "Rising",
        "Falling",
        "AffectedBy",
        "Submerged",
        "AtSurface",
        null
      ]
    },
    "ActionStateEnvelopeRange": {
      "description": "An inclusive numeric interval.",
      "properties": {
        "$type": {
          "const": "range"
        },
        "minimum": {
          "description": "The least admitted value.",
          "type": "number"
        },
        "maximum": {
          "description": "The greatest admitted value.",
          "type": "number"
        }
      },
      "required": [
        "minimum",
        "maximum"
      ],
      "additionalProperties": false
    },
    "ActionStateEnvelopeSet": {
      "description": "A closed numeric set. Values are authored labels encoded in the slot\u0027s deterministic numeric domain.",
      "properties": {
        "$type": {
          "const": "set"
        },
        "values": {
          "description": "The admitted values.",
          "type": "array",
          "items": {
            "type": "number"
          }
        }
      },
      "required": [
        "values"
      ],
      "additionalProperties": false
    },
    "ActionStateEnvelope": {
      "description": "The authored values a player-writable durable slot admits in this world.",
      "type": [
        "object",
        "null"
      ],
      "required": [
        "$type"
      ],
      "anyOf": [
        {
          "$ref": "#/$defs/ActionStateEnvelopeRange"
        },
        {
          "$ref": "#/$defs/ActionStateEnvelopeSet"
        }
      ]
    },
    "ActionStateSlot": {
      "description": "Declares one named body-state slot shared by every kit action in the world. The carrying WorldStateSection lane selects whether it belongs to the body or its identity.",
      "type": [
        "object",
        "null"
      ],
      "properties": {
        "name": {
          "description": "The stable slot name predicates and effects reference.",
          "type": "string"
        },
        "kind": {
          "description": "Whether the slot stores a counter or a remaining timer.",
          "enum": [
            "Counter",
            "Timer"
          ]
        },
        "initial": {
          "description": "The initial counter value or timer duration in seconds.",
          "type": "number",
          "default": 0
        },
        "resetFact": {
          "$ref": "#/$defs/ActionFactNullable",
          "description": "An optional body fact that resets the slot to Initial while it holds.",
          "default": null
        },
        "playerWritable": {
          "description": "Whether the identity driving the body may submit a value for the slot.",
          "type": "boolean",
          "default": false
        },
        "envelope": {
          "$ref": "#/$defs/ActionStateEnvelope",
          "description": "The visited world\u0027s admitted effective values. Required for a player-writable slot.",
          "default": null
        }
      },
      "required": [
        "name",
        "kind"
      ],
      "additionalProperties": false
    },
    "WorldThemeScrim": {
      "description": "One scrim\u0027s fill color plus its own alpha, split apart so a world can retheme opacity independent of hue \u2014 the two knobs a scrim (a translucent panel/strip/chip backing) actually varies. Alpha is clamped to ScrimMinAlpha at resolve time when it is a state binding (see WorldDefinitionValidator\u0027s theme validation for the literal-authoring floor).",
      "type": "object",
      "properties": {
        "color": {
          "description": "The scrim\u0027s opaque fill color."
        },
        "alpha": {
          "description": "The scrim\u0027s opacity, in [0, 1]."
        }
      },
      "required": [
        "color",
        "alpha"
      ],
      "additionalProperties": false
    },
    "WorldThemeBloomHue": {
      "description": "One elevation bloom hue\u0027s lit ring \u002B outer halo pair (see Puck.Overlays.DesignTokens.Elevation for the composite rule). Each color carries its own baked alpha (#RRGGBBAA).",
      "type": "object",
      "properties": {
        "ring": {
          "description": "The 1px lit ring color."
        },
        "halo": {
          "description": "The outer distance-falloff halo color."
        }
      },
      "required": [
        "ring",
        "halo"
      ],
      "additionalProperties": false
    },
    "WorldThemeCubicBezier": {
      "description": "One of the two settled cubic-bezier easings a theme\u0027s motion section authors.",
      "type": "object",
      "properties": {
        "x1": {
          "description": "The first control point\u0027s x.",
          "type": "number"
        },
        "y1": {
          "description": "The first control point\u0027s y.",
          "type": "number"
        },
        "x2": {
          "description": "The second control point\u0027s x.",
          "type": "number"
        },
        "y2": {
          "description": "The second control point\u0027s y.",
          "type": "number"
        }
      },
      "additionalProperties": false
    },
    "WorldMusicRow": {
      "description": "One music-score asset reference row \u2014 a puck.music.v1 document\u0027s stable name, its file path (relative to BaseDirectory, the same convention WorldAddonRow.ModulePath already establishes for a machine-local asset shipped beside the executable), and the SHA-256 hex64 pin of the referenced document\u0027s own canonical bytes. Never embedded: the document is loaded, canonicalized, and hash-verified where it is compiled (Server.WorldServer\u0027s construction), the same load-then-pin discipline Puck.Text.FontAtlasSourceResolver.ResolvePinnedContained already applies to a font asset.",
      "type": [
        "object",
        "null"
      ],
      "properties": {
        "name": {
          "description": "The row\u0027s stable name \u2014 its mutation address.",
          "type": "string"
        },
        "source": {
          "description": "The referenced document\u0027s file path.",
          "type": "string"
        },
        "hash": {
          "description": "The SHA-256 hex64 of the referenced document\u0027s canonical bytes.",
          "type": "string"
        }
      },
      "required": [
        "name",
        "source",
        "hash"
      ],
      "additionalProperties": false
    }
  }
}

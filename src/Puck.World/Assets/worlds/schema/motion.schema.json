{
  "type": [
    "object",
    "null"
  ],
  "properties": {
    "moveSpeed": {
      "description": "Locomotion speed in world units per second \u2014 the profileless fallback a stand-in advances on (a seated player whose identity CLAIMS a rate reads that claim instead, live, so identity.motion stays real-time; an identity claiming none rides the kit\u0027s own rate).",
      "type": "number"
    },
    "turnSpeed": {
      "description": "Turn speed in radians per second (the profileless fallback counterpart to MoveSpeed).",
      "type": "number"
    },
    "maxSmoothError": {
      "description": "The largest server-correction position error, in world units, that presentation may ease instead of snapping.",
      "type": "number"
    }
  },
  "additionalProperties": false,
  "default": null
}

{
  "type": [
    "object",
    "null"
  ],
  "properties": {
    "portalDefaults": {
      "description": "The travel default every portal facet in this document falls back to when it authors no Travel of its own.",
      "type": "object",
      "properties": {
        "travel": {
          "description": "The default travel scope (see WorldPortalTravel).",
          "type": "string",
          "enum": [
            "party",
            "body"
          ]
        },
        "holdSeconds": {
          "description": "How long a destination reservation is binding, converted from source simulation ticks through the exact 50400 engine-tick bridge.",
          "type": "number",
          "default": 2
        },
        "full": {
          "description": "Whether the client may retry after an immediate full-border refusal.",
          "enum": [
            "Retry",
            "Refuse"
          ],
          "default": "Retry"
        },
        "partyAllOrNothing": {
          "description": "Whether a party transfer reserves and commits as one cohort. When false, each member is a separate atomic body transfer.",
          "type": "boolean",
          "default": true
        }
      },
      "required": [
        "travel"
      ],
      "additionalProperties": false
    }
  },
  "required": [
    "portalDefaults"
  ],
  "additionalProperties": false,
  "default": null
}

{
  "description": "The cable-link rows (default empty) \u2014 groups of screens whose machines advance as one interleaved unit (see WorldScreenLink).",
  "type": "array",
  "items": {
    "description": "A cable-linked group of screens whose machines advance as one interleaved unit. The binder steps the link, never its members individually, so the engine\u0027s deterministic interleave \u2014 not the host\u0027s frame order \u2014 decides who runs when. Every member must resolve to a machine from the same engine, and that engine must implement IMachineLinkingEngine; a link whose members do not currently satisfy that is reported dormant, never silently dropped.",
    "type": [
      "object",
      "null"
    ],
    "properties": {
      "name": {
        "description": "The link\u0027s stable kebab-case name (its mutation address).",
        "type": "string"
      },
      "screens": {
        "description": "The engine screen indices in cable order (2 or more, no duplicates).",
        "type": "array",
        "items": {
          "type": "integer"
        }
      }
    },
    "required": [
      "name",
      "screens"
    ],
    "additionalProperties": false
  }
}

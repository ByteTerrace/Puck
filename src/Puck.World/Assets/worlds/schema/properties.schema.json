{
  "description": "The properties section (default null = none) \u2014 the carrier-property NAME vocabulary (see WorldPropertyRegistrySection), validated the SAME way a group kind name is: unknown- by-name. OPTIONAL, for the SAME reason Rules is.",
  "type": [
    "object",
    "null"
  ],
  "properties": {
    "names": {
      "description": "The declared property vocabulary \u2014 unique, non-empty, each naming a declared keyed int state row of the same name.",
      "type": "array",
      "items": {
        "type": [
          "string",
          "null"
        ]
      }
    }
  },
  "required": [
    "names"
  ],
  "additionalProperties": false,
  "default": null
}

{
  "type": [
    "array",
    "null"
  ],
  "items": {
    "description": "One placeable camera composed from a reference frame, local motion, framing policy, lens, and render target.",
    "type": [
      "object",
      "null"
    ],
    "properties": {
      "name": {
        "description": "The camera\u0027s stable name \u2014 the handle a View screen / layout slot samples by.",
        "type": "string"
      },
      "anchor": {
        "description": "What the camera rides, or null for the world reference frame.",
        "type": [
          "object",
          "null"
        ],
        "required": [
          "$type"
        ],
        "anyOf": [
          {
            "$ref": "./common.schema.json#/$defs/WorldAnchorEntity"
          },
          {
            "$ref": "./common.schema.json#/$defs/WorldAnchorEntityPart"
          },
          {
            "$ref": "./common.schema.json#/$defs/WorldAnchorPlacement"
          },
          {
            "$ref": "./common.schema.json#/$defs/WorldAnchorGroup"
          }
        ]
      },
      "rig": {
        "$ref": "./common.schema.json#/$defs/WorldCameraProgram",
        "description": "The independent local motion, aim, and lens axes."
      },
      "renderWidth": {
        "description": "The offscreen render width in pixels.",
        "type": "integer"
      },
      "renderHeight": {
        "description": "The offscreen render height in pixels.",
        "type": "integer"
      }
    },
    "required": [
      "name",
      "anchor",
      "rig",
      "renderWidth",
      "renderHeight"
    ],
    "additionalProperties": false
  },
  "default": null
}

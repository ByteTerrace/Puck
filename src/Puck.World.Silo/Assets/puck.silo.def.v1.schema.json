{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "puck.silo.def.v1",
  "x-puck": {
    "schemaVersion": "puck.silo.def.v1",
    "generator": "Puck.World.WorldSchema"
  },
  "title": "Puck world silo (puck.silo.def.v1)",
  "description": "The silo document (puck.silo.def.v1) \u2014 durable configuration for one Puck.World.Silo process: which worlds it may activate, its declared door budget, where its checkpoints/journals/definitions live, its own state directory, and its clustering membership. Loaded once at silo start (--silo \u003Cpath\u003E); nothing in it is simulation state. See WorldSiloDefinitionValidator for the checks a document must pass before the silo composes against it.",
  "type": [
    "object",
    "null"
  ],
  "properties": {
    "worlds": {
      "description": "Every world this silo may activate, keyed by World.",
      "type": "array",
      "items": {
        "description": "One world this silo activates. Its own host.authority (read from its definition after load) is the subject its federation key signs as \u2014 never this silo\u0027s own identity.",
        "type": [
          "object",
          "null"
        ],
        "properties": {
          "owner": {
            "description": "The owning identity\u0027s stable UUID.",
            "type": "string",
            "format": "uuid"
          },
          "world": {
            "description": "The world id under that owner\u0027s container \u2014 the grain key\u0027s own extension.",
            "title": "SafeName",
            "type": "string"
          },
          "federation": {
            "description": "This row\u0027s own signing key.",
            "type": "object",
            "properties": {
              "keyFile": {
                "description": "The path to the PKCS#8 private-key file this row signs its outbound federation claims and its own door\u0027s admission challenges with.",
                "type": "string"
              },
              "authentication": {
                "description": "Optional installed connection-identity provider, layered over this world\u0027s own federation credentials.",
                "type": [
                  "object",
                  "null"
                ],
                "properties": {
                  "type": {
                    "description": "The case-sensitive key in the composition root\u0027s extension registry.",
                    "type": "string"
                  },
                  "settings": {
                    "description": "An object validated by the selected extension before host startup.",
                    "$comment": "Open payload: this member\u0027s shape is decided by an id named elsewhere in the document, never by this generator."
                  }
                },
                "required": [
                  "type",
                  "settings"
                ],
                "additionalProperties": false,
                "title": "WorldSiloExtension"
              }
            },
            "required": [
              "keyFile"
            ],
            "additionalProperties": false,
            "title": "WorldSiloFederation"
          },
          "pinned": {
            "description": "Whether this row activates at silo start and never idle-deactivates.",
            "type": "boolean",
            "default": false
          }
        },
        "required": [
          "owner",
          "world",
          "federation"
        ],
        "additionalProperties": false,
        "title": "WorldSiloWorldRow"
      }
    },
    "doors": {
      "description": "The declared door budget.",
      "type": "object",
      "properties": {
        "budget": {
          "description": "The maximum number of Pinned rows this silo may activate at start.",
          "type": "integer"
        }
      },
      "required": [
        "budget"
      ],
      "additionalProperties": false,
      "title": "WorldSiloDoors"
    },
    "store": {
      "description": "The checkpoint/journal/definition backend.",
      "type": "object",
      "properties": {
        "type": {
          "description": "The case-sensitive key in the composition root\u0027s extension registry.",
          "type": "string"
        },
        "settings": {
          "description": "An object validated by the selected extension before host startup.",
          "$comment": "Open payload: this member\u0027s shape is decided by an id named elsewhere in the document, never by this generator."
        }
      },
      "required": [
        "type",
        "settings"
      ],
      "additionalProperties": false,
      "title": "WorldSiloExtension"
    },
    "stateDir": {
      "description": "The root every activated row\u0027s container-ephemeral owned-world store resolves its own directory under \u2014 the --state-dir counterpart.",
      "type": "string"
    },
    "clustering": {
      "description": "Orleans cluster membership.",
      "type": "object",
      "properties": {
        "kind": {
          "description": "The membership implementation selected at composition; this distribution supports Localhost.",
          "type": "string"
        }
      },
      "required": [
        "kind"
      ],
      "additionalProperties": false,
      "title": "WorldSiloClustering"
    },
    "lifecycle": {
      "description": "Optional deployment-owned host lifecycle adapter.",
      "type": [
        "object",
        "null"
      ],
      "properties": {
        "shutdownSeconds": {
          "description": "Maximum time allowed for operator or deployment retirement.",
          "type": "integer"
        },
        "healthPort": {
          "description": "HTTP health port; retirement requests are accepted only from loopback.",
          "type": "integer"
        },
        "observer": {
          "description": "Optional installed extension supplying host retirement deadlines; absent means no external observation.",
          "type": [
            "object",
            "null"
          ],
          "properties": {
            "type": {
              "description": "The case-sensitive key in the composition root\u0027s extension registry.",
              "type": "string"
            },
            "settings": {
              "description": "An object validated by the selected extension before host startup.",
              "$comment": "Open payload: this member\u0027s shape is decided by an id named elsewhere in the document, never by this generator."
            }
          },
          "required": [
            "type",
            "settings"
          ],
          "additionalProperties": false,
          "default": null,
          "title": "WorldSiloExtension"
        },
        "progressTimeoutSeconds": {
          "description": "Maximum wall-clock age of a completed simulation step before liveness fails.",
          "type": "integer",
          "default": 30
        },
        "checkpointTimeoutSeconds": {
          "description": "Maximum wall-clock age of a successful checkpoint before readiness fails.",
          "type": "integer",
          "default": 180
        },
        "journalTimeoutSeconds": {
          "description": "Maximum wall-clock interval without journal progress while appends are pending.",
          "type": "integer",
          "default": 30
        },
        "journalBacklogLimit": {
          "description": "Maximum outstanding journal appends admitted as healthy.",
          "type": "integer",
          "default": 1024
        }
      },
      "required": [
        "shutdownSeconds",
        "healthPort"
      ],
      "additionalProperties": false,
      "default": null,
      "title": "WorldSiloLifecycle"
    },
    "schema": {
      "description": "Gets the document schema tag \u2014 SchemaVersion for a well-formed document.",
      "type": "string",
      "const": "puck.silo.def.v1"
    }
  },
  "required": [
    "worlds",
    "doors",
    "store",
    "stateDir",
    "clustering"
  ],
  "additionalProperties": false
}

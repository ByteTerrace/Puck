namespace Puck.Launcher;

/// <summary>Stable names for the terminal-owned command surface.</summary>
public static class TerminalCommandNames {
    /// <summary>Shows, hides, or toggles a local seat's terminal console.</summary>
    public const string Console = "console";
}
